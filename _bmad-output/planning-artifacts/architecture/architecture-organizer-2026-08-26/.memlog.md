---
scope: Anti-Overwhelm Mobile Task Organizer — validation build (Android, single-user, local-first, BYOK Slicer)
purpose: Architecture spine for the validation build: fix the invariants that keep independently-built epics/stories from diverging. Inputs: prd-organizer-2026-08-20 (+addendum) and ux-organizer-2026-08-21 (EXPERIENCE.md, DESIGN.md). Greenfield, no code. Second deliverable: interactive HTML+SVG deck.
altitude: initiative -> features (whole system; PRD covers the full validation build)
updated: 2026-08-27T15:51
---

- (constraint) Inherited from PRD §1.1 principle 2 + §7 'No overdue at the schema level': no field, flag or derived value anywhere may express lateness, debt or a missed occurrence. This is a data-model constraint, not a UI rule — binds the schema, the domain model and the export format alike.
- (constraint) Inherited from PRD §7 'Offline by default — execution, not genesis': dispensing, completing, weaving, rescheduling, album, dashboard, export and Manual Capture (voice dictation included) must work with zero network, always. Only slicing/re-slicing may require it. The shipped Evergreen day is exempt because it was sliced at build time.
- (constraint) Inherited from PRD §7 egress map: exactly three payloads may ever leave the device toward an AI provider (scan image, project genesis text, rescue re-slice text) plus whatever the user exports to their own storage. Audio: nowhere. To the developers: nothing. No analytics SDK in the build.
- (constraint) Inherited from PRD §7 background minimalism: the ONLY background work in the build is FR-24's daily invitation. No sync job, no location, no persistent service, no wake lock, no microphone outside an explicit press. FR-30's automatic export is explicitly foreground-triggered (session end, app backgrounding), not background work.
- (constraint) Inherited from PRD §7 'Copy is the product surface': every string externalised, no runtime sentence concatenation, because SM-C2's anti-shaming audit must be reviewable as one flat string table. Binds every UI-facing module.
- (constraint) Inherited from PRD FR-28: the Slicer sits behind a swappable access interface with three implementations (Local / BYOK / Managed); the validation build ships BYOK usable plus a debug-only Local canned-slice stub, so the interface has two real callers. Provider allowlist frozen at build time, never fetched. Key in the OS keystore only, never in the export.
- (constraint) Inherited from PRD §7 latency contract: cold start to first dispensed card <= 2 s; Done -> next card < 500 ms; energy re-filter < 500 ms. Slicing is explicitly exempt (no cap, no timeout, foreground wait).
- (constraint) Inherited from PRD §7 + FR-30: one legible-text-plus-images format serves three uses simultaneously — FR-26 raw series export, validation data source, and backup/restore format with a full one-file-picker import (no merging, no partial restore).
- (constraint) Inherited from PRD FR-31: the Evergreen Library is a build-time data file whose coverage floor (>= 28 distinct 10-15 min non-daily entries) is verifiable WITHOUT running the app. Catalogue content in addendum A12: 34 daily, 36 weekly across 5 zones, 15 monthly/seasonal; 32 eligible units against a floor of 28.
- (constraint) Inherited from PRD §5.2 NL-1 + §1.1 principle 1 (scoped to pending-work enumeration): no screen anywhere lists, counts, filters or browses captured or pending tasks. Two declared exceptions only, E1 (Archetype Template/cluster list) and E2 (multi-path genesis zone). Binds the read side of any data access API — a query that returns the pending pool to a UI layer is the violation.
- (version) Kotlin 2.4.0 stable, released 2026-08-11 (next: 2.4.20 planned Sept 2026). Verified on the web 2026-08-26, not from training data.
- (version) Jetpack Compose BOM 2026.08.00 (Compose 1.12 across core modules). Verified on the web 2026-08-26.
- (version) Android 16 = API 36. From 2026-08-31 Google Play requires new apps and updates to target API 36. Validation build is sideloaded so Play is not a gate, but API 36 is the current target. Verified on the web 2026-08-26.
- (version) Gemma 4 E4B (Local path candidate, PRD OQ-1) confirmed real and shipping for Android via MediaPipe LLM Inference API on LiteRT-LM; litert-community/gemma-4-E4B-it-litert-lm on Hugging Face. Accepts text+image+audio. Q4_K_M ~2.5 GB on disk, 4-5 GB RAM at runtime — the RAM figure is the real handset constraint, not existence. Verified on the web 2026-08-26.
- (event) Run started, coaching path chosen. Second deliverable requested: interactive HTML+SVG deck. Greenfield: no code exists, no project-context.md.
- (decision) [ADOPTED by user, 2026-08-26] Platform: Flutter/Dart, not native Kotlin/Compose and not KMP. Architect's lean was native (every PRD guarantee resting on an Android API property rather than on our discipline); user chose Flutter and the choice stands. Trade-off accepted knowingly: iOS optionality and one codebase, paid for with a plugin layer between us and the OS properties FR-24/FR-25/FR-32 rest on. Consequence: the plugin boundary becomes an architectural invariant rather than an implementation detail — see the platform-channel AD.
- (version) Flutter 3.47.1 / Dart 3.13.1, stable 2026-08-19 (3.47.0 was 2026-08-12, Dart 3.13.0; previous line 3.44 was 2026-05-18 with Dart 3.12). Impeller is the default renderer on Android with Skia removed; Material and Cupertino have moved out of Flutter core into standalone packages — a dependency the pubspec must now name explicitly. Verified on the web 2026-08-26.
- (assumption) EVIDENCE AGAINST TRUSTING THE NOTIFICATION PLUGIN: flutter_local_notifications carried issue #2106 — Android notification channels created one importance level LOWER than specified (reported against 15.1.1). Whether or not it is fixed today, it is proof that FR-24's enforcement point (NotificationChannel importance fixed at creation, app can never widen it) is not directly observable through the plugin. Addendum A6 calls channel design 'the enforcement point' and says the property is what makes 'incapable of escalating' real rather than aspirational.
- (version) speech_to_text 7.4.0 (latest, ~3 months old as of 2026-08-26) does NOT expose Android's on-device recognizer and documents nothing about preventing cloud fallback; it wraps the generic device recognition service. minSdk 21. Also documented: Android enforces a very short pause timeout with no way to modify it, and the plugin is designed for short intermittent use — which happens to match FR-32's one-line capture. Verified on pub.dev 2026-08-26.
- (version) Android SpeechRecognizer.createOnDeviceSpeechRecognizer() FORCES on-device recognition and fails if no compatible local engine exists; SpeechRecognizer.isOnDeviceRecognitionAvailable() returns whether it will succeed. Both from API 31. This pair is an exact match for FR-32: 'no cloud fallback' becomes a property of the API chosen rather than a promise, and 'the microphone affordance is simply not present' becomes a single availability call. EXTRA_PREFER_OFFLINE is only a hint and is NOT sufficient for FR-32. Verified on the web 2026-08-26.
- (version) Gemma 4 E4B on-device via Flutter has no mature plugin path: MediaPipe LLM Inference API / LiteRT-LM are Android-native. If OQ-1 ever selects the Local path, it is a platform channel we write, not a package we add. Cost to name in Deferred rather than discover later.
- (decision) [ADOPTED by user, 2026-08-26] PARADIGM: functional core / imperative shell over an append-only act log. NOTHING about a day is persisted. Persisted: the task pool (shipped catalogue + manual captures + Slicer-authored epic steps) as immutable facts, and an append-only log of user acts. Today's composition, the next card, FR-5's decline count, the snowball's comfortable-day run, a capture's deal window and an Epic's buffered target date are ALL derived by a pure function of (pool, log, today, session state). Binds: FR-3, FR-4, FR-5, FR-6, FR-7, FR-9, FR-12, FR-13, FR-14, FR-23, FR-26, §7 no-overdue. Prevents: the divergence where one unit builds a stored daily_plan and another derives — they cannot coexist, and the stored one makes lateness expressible. Rationale: an append-only log of user acts can only record what the user DID; 'overdue' is a statement about what the user did NOT do, so it is not forbidden but inexpressible. This is PRD §1.1 principle 2's internal-signals clause ('they count only what the user did, never what they didn't') read as a storage contract. Corollary: FR-14's Silent Rescheduler is not code — nothing was ever assigned to a future day, so nothing needs re-planning; FR-6's Warm Return needs no code path of its own.
- (decision) Rejected alternative to the paradigm above: a stored daily_plan table (familiar, inspectable, easy to start). Rejected because after an absence it leaves rows on past dates in a pending state, and deciding what happens to those rows IS the overdue question that §7 forbids at schema level — plus the Silent Rescheduler becomes real mutating code whose first bug is a visibly wrong plan. Also rejected: derive-but-memoize-the-day, because within-day stability is already derivable from the log (it records what was dealt), so the cache would add the one place staleness could live.
- (decision) [ADOPTED by user, 2026-08-26] PLUGIN BOUNDARY: we write our own Android platform channel in exactly the two places where the PRD guarantee IS a property of an OS API, and use pub.dev plugins everywhere else. Own channel: (1) FR-24 notification — NotificationChannel at IMPORTANCE_LOW plus setShowBadge(false), created once, because addendum A6 makes channel-importance immutability the enforcement point and flutter_local_notifications issue #2106 proves a plugin can silently alter the level; (2) FR-32 dictation — createOnDeviceSpeechRecognizer() and isOnDeviceRecognitionAvailable(), because speech_to_text 7.4.0 exposes neither and EXTRA_PREFER_OFFLINE is a hint, while FR-32 forbids any cloud fallback outright. Plugin-served: camera, ML Kit on-device face detection, OS keystore, SAF folder picker — in all four the guarantee is our own behaviour (we do not transmit), not a system property a plugin could degrade. Prevents: a story wiring FR-24 or FR-32 through a convenient package and quietly voiding the guarantee the FR rests on.
- (decision) Rejected: platform channels for all five OS touchpoints (buys nothing in the three where a plugin cannot break the guarantee, and spends most of what choosing Flutter was meant to save). Also rejected: everything via plugin verified by integration tests — a test verifies today's plugin version, and issue #2106 arrived in an update.
- (direction) User delegated Dart state management to the architect (2026-08-26): to be fixed as a CONVENTION (seed), not an invariant — the code owns it once it exists. The only genuinely binding rule in that area is that the UI layer has no API through which it could obtain the pending pool: NL-1 lives in the shape of the read side, not in the screens.
- (decision) [ADOPTED by user, 2026-08-26] FR-28 GATE narrowed to written NO-TRAINING only; retention dropped from the gate entirely. User's ruling verbatim in spirit: 'En el momento que es el usuario quien proporciona la key, nos desentendemos completamente del problema porque eso esta bajo su responsabilidad... deberiamos descartar el hablar de retencion, con lo de no-entrenamiento debe de bastar de sobra... No podemos ser mas papistas que el Papa.' Consequence: the app states ONCE, at key entry in settings, that a free-tier key may be used for training and that the app cannot tell which tier a key belongs to; then never again, anywhere. Prevents: a story adding a recurring warning, a badge, or a Dispenser-visible provider state in the name of privacy diligence.
- (constraint) WEB-VERIFIED 2026-08-26, the finding that forced the gate change: no provider reachable with a user's own key offers written zero retention. OpenAI API — no training by default, ~30 days abuse retention, ZDR only on Enterprise. Anthropic API — commercial/API data explicitly excluded from training, abuse retention applies. Gemini PAID API/Vertex — no training, ~72 h retention (shortest). Gemini FREE tier (AI Studio / unpaid quota) — content IS used to improve Google products, i.e. training, and human reviewers may see it. The free/paid split is exactly where no-training discriminates, which is why no-training alone is a sufficient gate and retention is not.
- (decision) [ARCHITECT'S CALL per OQ-10 assignment] Initial allowlist, frozen at build time: Google Gemini (paid API), OpenAI, Anthropic. Three entries rather than one so the user can use a key they already hold — which serves A8's 'never hesitate before shooting a photo' better than a single-provider list. Each entry states provider name + no-training terms + verification date 2026-08-26 + that verification was on that date and not since. Selection rested on the no-training gate and structured-output reliability only.
- (decision) [ADOPTED, follows from the user's ruling] OQ-10 stale-terms half: NOTHING automatic. No age indicator, no re-check, no removal mechanism. The dated statement FR-28 already requires is the whole mechanism and shipping a build is what corrects it. Rejected: an age display in settings ('verificado hace 11 meses') and an outbound link to the provider's terms — both plant a small piece of pending work in the one surface §7 reserves for the validator, for a vigilance the app does not have.
- (decision) A8 CLOSED with a number, as the addendum asked. A room photo at ~1 MP is ~1,200 input tokens; Gemini 3.7 Flash at USD 0.75/1M input, GPT-5.5 at USD 5.00/1M => roughly USD 0.003-0.006 per scan including a ~500-token micro-plan out. At 1-2 scans/week over the 4-week window: cents in total, far inside A8's 'a coffee' range. Therefore cost DISCRIMINATES NOTHING between candidates and is removed as an allowlist criterion. Prices verified on the web 2026-08-26.
- (decision) OQ-8 CLOSED: FR-24's daily invitation is delivered by an INEXACT daily alarm, and no new permission is requested for it. Reasoning is policy, not preference: USE_EXACT_ALARM is granted on install but Play-restricted to apps whose core user-facing function requires precise timing (alarm, timer, calendar) — this app's Floating Time Bag is the negation of that; SCHEDULE_EXACT_ALARM is denied by default from Android 14 and needs a trip to a system settings screen, i.e. a permission prompt for a content-free ping, against §7 background minimalism. And FR-24's own copy rule pays for it: the notification carries 'no time reference other than the offered minutes', so a notification with no clock inside it cannot be wrong about the clock. Rejected: WorkManager periodic work (heavier than a once-a-day alarm and buys nothing here). Note: alarms are lost on reboot, so RECEIVE_BOOT_COMPLETED (a normal, auto-granted permission, not a runtime one) reschedules — this does not touch FR-32's claim to be the build's one new RUNTIME permission. Verified on the web 2026-08-26.
- (decision) [ARCHITECT'S CALL] Egress layer enforces a single image-resolution cap before any upload. One rule serving three requirements at once: FR-25's upload minimization, cost, and privacy (less detail of the user's home leaves the device). Binds: FR-16, FR-25. Prevents: each call site choosing its own resolution, and the cap silently drifting upward for output quality.
- (decision) [ADOPTED by user, 2026-08-26] The Local path stays as-is and is never treated as the answer to a privacy or cost problem: 'no todos los telefonos pueden ejecutar un modelo o los usuarios quieren dedicarle ese espacio de almacenamiento'. So it remains FR-28's debug-only canned-slice stub in this build, and the real on-device path is Deferred with its cost named (a Kotlin platform channel over MediaPipe LLM Inference / LiteRT-LM, plus Gemma 4 E4B's ~2.5 GB disk and 4-5 GB runtime RAM). Rejected explicitly: shipping Local as the validation path in order to satisfy the old literal no-retention gate.
- (event) Paid the gate change upstream 2026-08-26: edited prd.md (FR-25 bullet, FR-28 two bullets + one new bullet, OQ-1(a), OQ-10 closed, §10.2 assumption, §8 annex aggregator caveat, Changelog entry), addendum.md (A2, A9), and ux-designs EXPERIENCE.md (AI Access Path). Zero occurrences of no-retention remain in any of the three. OQ-8 also marked closed in the Changelog.
- (version) drift 2.34.3 (published ~2026-07-28), Flutter Favorite, verified publisher simonbinder.eu; companions drift_flutter and drift_sqflite (drift_sqflite last updated 2026-05-18). Supports schema migrations and raw SQL, which is what the acts-table insert-only trigger needs. Verified on pub.dev 2026-08-26.
- (decision) [ADOPTED by user, 2026-08-26] THE DOMESTIC DAY: a day is [04:00 local, 04:00 local next). One definition, one function, and every day-or-24h question in the product routes through it — FR-5's 'declined on 3 different days', FR-7/FR-12's one Focus Chunk per day, FR-12's capture deal window, FR-24's at-most-one-per-24h, FR-31's weekly zone rotation, SM-1's '>=70% of days'. Prevents the incompatibility where one unit uses midnight and another uses a rolling 24 h, and the concrete failure it removes: dishes finished at 23:55 and a second Focus Chunk offered at 00:05 because 'it is another day' — a marathon with a comma (FR-12). Rejected: local midnight (splits a night session and duplicates the one-per-day window across 23:xx/00:xx) and a day that rolls on first app open (breaks SM-1, whose whole point is counting days the user did NOT open, and makes FR-5's counter depend on the behaviour it measures).
- (decision) [ADOPTED by user, 2026-08-26] DEPLOYMENT: signed APK sideloaded to the three validation handsets, same signing keystore for every build so install-on-top preserves data and the drift migration runs. FR-30's import is promoted from emergency function to the actual update path: export on all three phones, install on top, and import if a migration fails. Consequence that pays for itself — the restore path gets exercised three or four times during the window instead of never, which is what §7 meant by 'without an import, restore format is a promise rather than a property'. Rejected: disposable data between builds (SM-1 needs sessions across a 4-week window and SM-2 needs the week-1 baseline — losing data mid-window does not break the build, it breaks the validation) and Play internal testing (drags Google into the circuit of a build whose whole claim is that it has no backend, and adds a store listing and declared data policies).
- (decision) [ADOPTED by user, 2026-08-26] CRASH VISIBILITY: a local crash log, written to a file that travels in the export's DERIVED half, carrying stack and timestamp and no task text and no image paths. Zero egress: it transmits nothing and is not an SDK; it reaches the builder only if the person hands over their export. Accompanying rule, which is the real point: no third-party SDK may ever be added on the grounds that it is not analytics — §7's egress map is a closed list, and crash reporting is egress to the developers, which §7 sets to nothing. Rejected: accepted blindness with nothing at all (a reproducible crash on a phone that is not the builder's would be undebuggable, and all he would have is that the person stopped using the app) and Crashlytics/Sentry (named explicitly so the option does not survive as an unnamed temptation).
- (decision) [ARCHITECT'S CALL] DERIVATION IS DETERMINISTIC AND FREE OF RANDOMNESS. Ties broken least-recently-dealt, then stable id order. Binds FR-3, FR-4, FR-12, FR-31. Prevents: a random pick, which would make the act log non-replayable and so strip the export of its standing as validation evidence — and would also make FR-31's coverage floor ('28 dealt Focus Chunks never repeat') unprovable, since the floor requires the rotation to be KNOWN rather than sampled.
- (decision) [ARCHITECT'S CALL] FR-25's per-scan consent becomes a SINGLE-USE TOKEN bound to one specific image, required by the egress function's signature. With it, 'a blanket always-allow setting does not exist' stops being forbidden and becomes impossible: there is no way to reach the upload path without a token for that scan, and a token cannot be reused. Prevents a story caching consent for convenience.
- (version) SAF for FR-30's persisted folder URI: shared_storage is DISCONTINUED (last update ~16 months before 2026-08-26). The live option is saf_util (SAF pickers, persisted permissions, streamed read/write). file_picker alone does NOT persist the grant. Android holds 512 persisted grants on API 30+. Risk to name: saf_util is a small community package and is the single point of failure for FR-30. Verified on the web 2026-08-26.
- (version) google_mlkit_face_detection (FR-25's on-device pre-upload refusal) exists and wraps ML Kit's native on-device API, but is explicitly NOT sponsored or maintained by Google — community-maintained; pub.dev page last updated Feb 2026, version number not obtained. ML Kit is 64-bit only (no armv7/i386). Risk named rather than assumed away. Verified on the web 2026-08-26.
- (decision) [ARCHITECT'S CALL] minSdk 31, target API 36 (Android 16). minSdk is set by FR-32: createOnDeviceSpeechRecognizer() and isOnDeviceRecognitionAvailable() are API 31. Accepted cost: handsets below Android 12 are excluded, which is immaterial for a three-phone validation and is a real constraint to revisit before any public phase.
- (event) Reviewer Gate run 2026-08-26. lint_spine.py: 4 low (unfilled {planning_artifacts} tokens in sources) -> fixed, re-run clean at 0 findings. Three lenses run SEQUENTIALLY and written to reviews/ rather than dispatched as parallel subagents, because this session's system instructions say not to call the Agent tool unless the user requests it, and the user had not; the reference's stated fallback was followed. NOTE: this was misattributed to the user in the session's closing summary and corrected — it is a session-level default, not the user's instruction, and a request from the user lifts it. Verdicts: verification PASS with 3 corrections; adversarial FAIL as drafted with 7 incompatibilities; rubric PASS on structure with 3 under-decided dimensions. Plus an inline reconcile pass against all four inputs: 3 misses.
- (decision) Reviewer fix (verification C1): minSdk raised 31 -> 33. Sources conflict on whether createOnDeviceSpeechRecognizer() arrived at API 31 or 33 and the official annotation could not be confirmed. 33 removes the ambiguity at no cost (Android 13 is from 2022). The reason this mattered: at minSdk 31, if 33 is correct, FR-32 would be silently absent on Android 12-12L and FR-32's own 'the affordance is simply not present' rule would MASK the defect, so it would never surface as a bug.
- (version) Reviewer fix (verification H1): flutter_secure_storage verified rather than asserted — 11.x, actively maintained, migrated off deprecated Jetpack Crypto to Google Tink, RSA-OAEP + AES-GCM with the AES key in Android KeyStore, min Android SDK 23, needs Dart >=3.3 / Flutter >=3.19. Maintained fork flutter_secure_storage_x 10.2.3 exists as fallback. Verified 2026-08-26.
- (decision) Reviewer fix (verification M1): material and cupertino are now explicit pubspec dependencies — they left Flutter core in 3.44 and no longer arrive with the flutter dependency. A cold-start pubspec assuming otherwise will not resolve.
- (decision) Reviewer fix (adversarial A1) -> AD-19: there is no session of record. A session is session_started with no matching session_ended in the current domestic day. The hole: AD-1 listed 'session' among the derivation's inputs and never declared it derived, so one unit would hold it in a provider and another rebuild it from acts — and after process death mid-session the first deals a fresh card while the second resumes the pocket, which is what FR-9 mandates.
- (decision) Reviewer fix (adversarial A2) -> AD-20: core/weave is the only code that may emit a deal; everything else returns candidates with precedence. The hole: FR-12 gives the Focus Chunk slot to the Epic/zone/fondo AND gives a pending 10-15 min capture precedence, so the Weaver and the capture logic each had a claim and no AD arbitrated — two large items in one day arriving legitimately from two places.
- (decision) Reviewer fix (adversarial A3) -> AD-5 amended: adapters return inert DTOs and never domain objects; only the core constructs a pool item, an act or a period. Closes the class rather than the instance: an adapter holding a Slicer response would tag origin by how the data arrived while the core tags it by lineage, and AD-14's rescue-inherits-origin rule would lose silently, shifting SM-4's arithmetic.
- (decision) Reviewer fix (adversarial A4) -> AD-13 amended: the album manifest is DERIVED, not authoritative. An independently editable manifest would let one unit delete by removing a row while another deletes by appending an act, and restore reads the authoritative half, so the contradiction would be load-bearing.
- (decision) Reviewer fix (adversarial A5) -> AD-8 amended: the consent_granted act is instrumentation only (FR-26 series b) and carries no capability; the token is never persisted, exported or reconstructible. Otherwise a unit reconstructing a token from the log — reasonable, since the log is the source of truth for everything else — restores the blanket always-allow FR-25 forbids.
- (decision) Reviewer fix (adversarial A6) -> AD-1 amended: setting changes are acts and SETTINGS is a derived cache. With settings mutable and unlogged the export could not say what the Time Bag was on day 5, so a validation reading would not be reproducible from the export — contradicting AD-13's whole purpose. Bonus: removes the last exception to a uniform substrate.
- (decision) Reviewer fix (adversarial A7) -> AD-17 amended: at most one invitation per DOMESTIC day, not per rolling 24 h. With a chosen hour of 03:30 the two clocks disagree about whether two invitations fell on one day.
- (decision) Reconcile fix (R1) -> AD-4 widened from 'one definition of a day' to ONE CALENDAR AUTHORITY owning day, week and season. The inputs depend on a week (FR-31's zone rotation; SM-2's 'persisting until answered that week') and a season (FR-15's 'once per season per project') and neither was defined anywhere. Week anchored Monday, so SM-2's Sunday is the week's last day; season = three-month meteorological quarters on domestic-day boundaries.
- (decision) Reconcile fix (R2) -> AD-13 amended: the export adapter reports its outcome to the settings surface and to nothing else — no toast, no badge, no backup-age line. FR-30 requires settings-only visibility and EXPERIENCE.md makes the absence of any other trace a specified behaviour; AD-13 previously covered the format and said nothing about who may observe the outcome.
- (decision) Reconcile fix (R3) -> AD-2 act vocabulary gains item_triaged carrying the destination and an optional coarse volume tag (bolsa / caja / caja grande / mueble, never a number — FR-22), plus setting_changed.
- (decision) Rubric fix (G1): i18n mechanism decided as a convention — ARB via flutter_localizations/gen_l10n, Spanish only, no runtime-computed keys, so the generated Spanish ARB IS the flat audit table AD-15 requires. One controlled exception to no-concatenation: ICU plural/number placeholders inside an otherwise fixed sentence. Second locale named in Deferred.
- (decision) Rubric fix (G2): the cold-start budget gets an owner — §7's <=2 s to first card and <500 ms Done->next are asserted by tests over the core's derivation; the catalogue asset is parsed lazily and the derivation reads only the log slice the current periods need. Previously an architectural property with no owner, so two units could each add first-run work in good faith.
- (decision) Rubric fix (G3): screen-reader behaviour named in Deferred. Unlike the UX copy questions it has a structural half — traversal order across a one-card surface plus an ambient container.
- (event) All reviewer and reconcile fixes applied to the spine. lint clean. 20 ADs.
- (event) Deck delivered: deck.html, 9 plates, 20 AD cards filterable by family, 4 hand-authored SVG diagrams. Designed IN the product's own authored system rather than a new identity — Aliento palette verbatim from DESIGN.md, DESIGN.md's paired type rule honoured (Lora for what a person wrote, Lexend for mechanism), and the signature is DESIGN.md rule 2: the colour plate misregistered on one global vector, in proportion to the element and never a fixed value. Dark mode does not invert: the plate CHANGES SHAPE to a 6px edge bar, which is DESIGN.md's own authored ruling for the destination marks. The three destination hues deliberately unused as fields, per the system's own prohibition. Published at https://claude.ai/code/artifact/b1fd6df1-6718-4e18-b623-a9ff491dc78b
- (event) spine finalized
- (event) REAL REVIEWER GATE run 2026-08-26 at the user's explicit request, lifting the session default against the Agent tool. Three independent subagents against ARCHITECTURE-SPINE.md, none with sight of this conversation, each writing to reviews/independent/. Verdicts: adversarial FAIL as final (17 incompatibilities, 5 critical); rubric ACCEPT WITH FIXES (2 critical); verification CHANGES REQUIRED (2 critical). Deduplicated: 8 critical, 12 high, 11 medium. CONCLUSION WORTH KEEPING: the independent gate substantially outperformed the author's own inline pass. The author's 7 findings were a subset of theirs, and THREE of the worst new findings were introduced BY the ADs the author added while fixing the first seven (AD-19's day-boundary evaporation, AD-1's settings-as-acts opening a path for the API key into the export, and the act vocabulary having no room for FR-26's machine-initiated events). An author reviewing their own work finds the seams they already suspected, not the ones they opened while sewing.
- (decision) PARADIGM SHARPENED by adversarial finding 5. The invariant was stated as 'a log of what the user did cannot express what the user did not do'. Too narrow: FR-26 series (b) and (d) require four machine-initiated events (consent declined, face refused, invitation emitted, app opened) and SM-C3's only stated test dies without them. New formulation in AD-21, which is strictly better: the log holds user acts AND system events, and NO ENTRY MAY ASSERT AN ABSENCE OR AN OBLIGATION. invitation_emitted is a fact; app_opened is a fact; overdue is neither and still does not fit. Also kills the third store (SharedPreferences for the last emission) that would otherwise have been invisible to the export and to AD-13's round-trip.
- (decision) New ADs from the independent gate: AD-21 (log holds user acts + system events, no entry asserts an absence; seven enumerated system events; no fourth store), AD-22 (no secret is ever an entry, pool fact or export content — closes the path by which a reasonable story exported the user's provider key into a possibly Drive-synced folder), AD-23 (the substrate has a theory of its own evolution: unknown kinds tolerated, payloads additive-only, catalogue ids permanent with an id-diff check — the risk AD-18 created and nothing covered), AD-24 (one EligibleDay predicate — FR-12's capture window and FR-5's decline counter are the same mechanism per the PRD and were derived twice), AD-25 (pool membership derived, no tombstone, no synthetic completion — FR-5 requires two items to leave the pool and no rule said how), AD-26 (the validator surface is the one place a number may cross — splits AD-6's over-broad opacity rule, which contradicted FR-23's dashboard).
- (decision) Amended by the independent gate: AD-1 (no mutable record anywhere — the conventions row contradicting the ERD note was the spine's own self-contradiction, caught by two lenses); AD-3 (card_dealt written by the answering command, never by a read, plus a purity CI check); AD-4 (zone offset stored per entry so travel does not re-date history; energy is day-scoped not session-scoped; the Notifier port takes an absolute instant so no native date arithmetic exists); AD-6 (decisions named as facts, never verbs, and they are inputs to weave not outputs to the shell); AD-7 (three seals, not one: Dart imports, Gradle graph, merged manifest — a Dart check cannot see a socket in our own Kotlin channels); AD-8 (consent binds to the scan's cache subdirectory; both scan files unlinked when the scan resolves BY ANY MEANS, since 'once a plan exists' never fires on the four failure paths); AD-11 (checkRecognitionSupport/triggerModelDownload gate, because service availability is not Spanish-model availability); AD-12 (crash records are system events, so they survive restore and need no third store); AD-13 (album bytes unlinked with the act, single-writer lock, temp-and-rename, torn-export test case); AD-15 (the ARB IS the table — there were two homes; audit list is every ARB key minus reviewed exclusions; the seven pinned by key with a sign-off marker, because existence is not review); AD-16 (four fields including a permanent id, Spanish copy in the ARB keyed by id, and a core rotation test because the build check counted the shipped file while FR-31's floor is about the eligible pool curation moves at runtime); AD-17 (RECEIVE_BOOT_COMPLETED named, permission refusals as system events); AD-19 (session belongs to the domestic day of its OWN start; declared pocket = start + extensions; session_ended has exactly three emitters); AD-20 (occupancy vs identity — a skip re-resolves identity and consumes no rotation; explicit fallback when curation drops the pool below the floor).
- (version) STACK CORRECTIONS from the verification lens, all web-verified 2026-08-26. FABRICATED AND REMOVED: the material/cupertino dependency row — those package names were invented from a search headline. Reality: decoupling landed in 3.47 not 3.44, the opt-in packages are material_ui 1.1.0 / cupertino_ui, the SDK still ships the libraries, and package:flutter/material.dart is only SCHEDULED for deprecation in the Nov 2026 stable. Nothing to add to the pubspec today. SETTLED AGAINST THE SPINE: createOnDeviceSpeechRecognizer() and isOnDeviceRecognitionAvailable() are API 31 per Google's own API-31 diff report; API 33 adds only checkRecognitionSupport, triggerModelDownload, ERROR_CANNOT_CHECK_SUPPORT. minSdk 33 survives on better grounds (POST_NOTIFICATIONS, and the model-presence pair FR-32 actually needs). CORRECTED: saf_util 3.1.0 provides NO read/write — saf_stream 4.0.1 is the write path; flutter_secure_storage 11.0.0 did NOT move to Tink (v10 rejected it for lacking biometric support) and under the default RSA-OAEP cipher the AES key is RSA-wrapped in SharedPreferences, which FR-28 forbids in words, so the AES-in-KeyStore cipher must be configured explicitly; Gemma 4 E4B is 3.66 GB / 3654 MB Android model size, not ~2.5 GB; flutter_gemma 1.6.5 exists with MediaPipe + LiteRT-LM engines and Gemma 4 E4B listed, contradicting 'no mature Flutter package exists'. ADDED: drift triggers only declarable in .drift files; Flutter 3.47 brings Java 17 and min Android API 24; API 37 shipped 2026-06-16; ML Kit abiFilters must exclude 32-bit ABIs; pinned flutter_riverpod 3.4.2, camera 0.12.0+2, google_mlkit_face_detection 0.15.1, uuid 4.6.0; Kotlin 2.4.0 was released 2026-06-03 (an earlier memlog entry said 2026-08-11 — wrong).
- (event) Spine rewritten rather than patched: 26 ADs (AD-1..AD-26, none renumbered), 20 conventions, 4 diagrams, lint clean at 0 findings. Deck republished with the corrected Stack, the six new AD cards, and plate 06 rewritten around the independent gate's findings. Prior author-written reviews kept at reviews/ alongside reviews/independent/ so the difference between the two passes stays visible.
- (event) spine finalized (second pass, after independent gate)
- (decision) Update 2026-08-27: validate-gate findings (reviews/validate-2026-08-27/) rolled in. ADV-1 critical closed: ONE LEDGER - a card_done closes the Focus-Chunk slot of the day its dealing session belongs to (AD-19), never the crossed-into day; a session whose own day slot is closed may not be dealt a Focus Chunk at all (AD-19, AD-20 amended). AD-24 amended: an eligible day carries a session STARTED on that day by its start instant; a session outliving its day does not make the later day eligible.
- (decision) ADV-2 closed: AD-24 amended - that day energy at a session = the last energy_set at or before that session start instant, defaulting green; a size is excluded on a day iff excluded at the start of every session that day; an energy_set belongs to its own instant day regardless of session attribution; AD-4 last-of-day rule governs the live pool only; the green default is derived, never written.
- (decision) ADV-3 closed: AD-21 enumeration extended to eight - export_recorded (outcome + cause), the durable home FR-30 observability needs. User-act vocabulary gains scan_abandoned (the user leaving the scan surface, an AD-8 resolution cause), closing the FR-26 series (b) denominator ambiguity. app_opened stays; _opened added to the sanctioned suffixes. permission_refused gets one stated reader: the derived permissionMayBeAsked fact (AD-17 never-ask-again).
- (decision) ADV-4 closed: AD-13 import contract - the import applies AD-23 tolerance (unknown kinds and fields preserved verbatim, defaulted where a default exists); refusal reserved for structural corruption and nothing else; property test extended with a build N+1 fixture imported into build N.
- (decision) ADV-5 closed: AD-25 amended - dissolution retires the original AND every not-yet-completed step of the chain, atomically; AD-20 amended - a completed rescue chain closes the slot of, and consumes rotation on, the day of its last card_done regardless of that day energy.
- (decision) ADV-6/7/8/9 closed: AD-4 - SM-2 report window is Sunday only, expires at the week boundary, never carries. AD-26 - the quiet affordance is unique, lives in the Dispenser chrome, only route from any surface within three taps. AD-19 - FR-3 early close is cause 1 (the tap emits session_ended); a fully-elapsed pocket closes the session at the first foreground instant. Conventions - ordering derivations read act instants never id bits; ids minted at commit, preserved verbatim by export/import.
- (decision) Rubric F1/F2/F3 closed: AD-2 insert-only triggers extended to the pool table (AD-14 citation now holds). ERD captioned - CATALOGUE_ENTRY a build-time asset, ALBUM_ENTRY a derived read model, QUARANTINE_BOX log-reconstructed with follow-up derived from box_created instant. tool/ gains the store seal (sixth check, AD-21: no persistence API outside Store and Folder adapters); guard inventory recounted; AD-22 CI grep placed; every-AD-has-a-test softened.
- (decision) Rubric lows + verification low closed: AD-20 records the FR-7 override (dealt read as answered Hecho); AD-24 gains warmReturnDue (48 h wall-clock since the later of last app_opened and last user act); AD-20 fixes multi-epic arbitration (least-recently-served active Epic); AD-16 - curation changes take effect at the next week boundary; conventions gain a shell-testing row (manual on validation handsets per AD-18); Stack - ML Kit 32-bit libs exist but only 64-bit are 16 KB-aligned; secure-storage Tink motivation dropped (unverifiable); API-37 Play requirement marked projection.
- (decision) Update round 2 (gate re-run findings, reviews/update-2026-08-27/): ADV-10+M-3 closed - the rescue chain now closes the slot of the day the session that dealt its last card_done belongs to (one attribution rule in AD-20, stated once), with the FR-31 red-day rotation override recorded deliberately. ADV-11+H-1 closed - AD-24 rewritten: eligible day = a session started on it AND at whose start at least one of that day sessions found the size not excluded; the energy anchor is day-scoped (last energy_set of that session OWN domestic day at or before start, defaulting green at the boundary), so yesterday red never freezes today windows.
- (decision) ADV-12+H-2 closed - AD-21 now states THREE reader exceptions (permissionMayBeAsked from permission_refused; warmReturnDue from app_opened; the validator surface renders export_recorded). ADV-13+ADV-19 closed - the one-ledger rule covers every card_* act of a session, dealt, done or skipped, chunk-class or not. ADV-14+H-3 closed - store seal names three adapters (Store, Folder, Files owning app-private bytes), catalogue asset loaded by a named shell loader on the allowlist, no adapter-private store; the SAF destination rides in setting_changed, the export redacts that one field, and the round-trip property excludes it from read-model identity (AD-22).
- (decision) Mediums+lows closed: scan_abandoned covers backgrounding too and the cache sweep runs at each app_opened (ADV-15); malformed defined as unparsable JSON or known kind lacking a field with no default, both shapes in the property corpus (ADV-16); export renames ordered images-pool-log with the log rename as commit point and import refuses any remaining temp name (ADV-17); settings renders latest success AND latest failure with cause (ADV-18, L-3); invitation hour interpreted in the offset in force at each reschedule - it follows the user (ADV-20); suffixes gain _declined and _failed (M-1); guard claim softened to name the review-enforced ADs (M-2); curation deferral scoped to weekly zones, daily and fondo take effect immediately (M-4); Deferred API-37 marked projection (L-1); FR-20/FR-22 bound to AD-26 (L-2); Due-as-fact-suffix declared outside the forbidden-vocabulary ban (L-4); uuid monotonicity marked opt-in (verification F1).
- (decision) Confirmation pass residuals closed: AD-6 gains the state-fact crossing clause (warmReturnDue may reach the shell for a non-work surface); AD-17 mirror fixed to 'one of AD-21 stated exceptions'; store seal and tree/diagram updated to name the Files adapter (port list, files/ module, A7 node); EligibleDay witness restricted to sessions starting no earlier than the item pool-fact creation (NS-1); temp-suffix convention and next-export healing stated in AD-13 (NS-2); SAF redaction form pinned to null-never-dropped (NS-3).
- (event) spine update finalized 2026-08-27 (second update pass): all 9 validate findings + 15 gate re-run findings + 4 confirmation residuals applied; AD IDs stable (AD-1..AD-26, none renumbered); confirmation pass 21/22 closed then residual applied; lint clean at 0 findings; stack re-verified live 2026-08-27 (verification lens PASS, all pins current).
- (decision) [ADOPTED by user, 2026-08-27] AD-13 EXPORT COMMIT: each export has a generationId; pool and log are immutable generation snapshots; album images are immutable content-addressed blobs shared across generations; a committed manifest written last names the generation, complete inventory, sizes and checksums. Import scans manifests newest-to-oldest and restores only the first generation whose complete inventory verifies; incomplete, partially visible or corrupt generations are ignored without invalidating the prior one. Retain the two newest verified generations; only after commit, garbage-collect bytes unreachable from both. No correctness claim relies on SAF rename atomicity. The property test cuts after every write and covers partial visibility, missing bytes and checksum mismatch. Binds AD-13/FR-30/AD-18; prevents compliant export and import units producing or accepting a mixed-vintage restore after process death, revoked permission or provider-specific SAF rename behavior.
- (decision) [ADOPTED by user, 2026-08-27] AD-22 CREDENTIAL CAPABILITY: setting_changed persists selectedProvider but never keyExists. Key material remains only in the OS keystore behind a CredentialVault port whose boundary exposes save, delete and availability, never secret bytes to the core. providerConfigured is selectedProvider != null AND live credentialAvailable(selectedProvider); availability is an installation-local capability queried live, never persisted, logged or exported. After restore the provider choice may remain while configuration is false until a credential is present. Round-trip identity excludes credential availability. Tests cover present, absent, deleted and Android-invalidated credentials; fixtures/CI reject both secret material and persisted keyExists=true. Binds AD-1/AD-5/AD-9/AD-13/AD-22/FR-28/FR-30; prevents restored settings and egress from disagreeing about whether a usable credential exists.
- (decision) Reviewer-gate tightening of adopted AD-13: Store produces pool+log from one SQLite read transaction under a process-wide foreground coordinator shared by export/import/cleanup/album mutation; generation order is (monotonic sequence, UUID), manifests name a verified predecessor, inventory excludes the manifest, exact bytes use SHA-256, create-once names never overwrite, partial SAF documents are ineligible, and uncertainty skips cleanup. Prevents logically torn but byte-valid snapshots, divergent newest selection, import/GC races, circular manifests and unsafe deletion under partial provider visibility.
- (decision) Reviewer-gate tightening of adopted AD-22: Android Keystore holds only a non-exportable wrapping key; provider API credentials are provider-scoped ciphertext envelopes in app-private Files storage, never preferences. Plaintext flows only shell credential handler -> vault and vault -> ByokSlicer transiently, never core; availability means decryptable now, with save/replace/delete/invalidation/TOCTOU semantics tested. AD-1 and AD-21 distinguish replayable domain state from these closed installation capabilities. Prevents an impossible arbitrary-string-in-Keystore implementation, secret leakage through core, alias-only false readiness and a fourth hidden store.
- (decision) Second reviewer-gate tightening: SAF listings cannot prove global completeness, so AD-13 retains at least the newest verified generation and predecessor but automatically deletes no committed manifest, snapshot or content blob; only locally named debris from an uncommitted attempt may be collected. Manifest wire shape, roles, contained paths, UUID/sequence and SHA-256 encoding are canonical. Committed-generation compaction is Deferred. Prevents hidden sibling generations and provider visibility delay from turning cleanup into data loss.
- (decision) Second reviewer-gate tightening: flutter_secure_storage removed; AD-11 now owns a third Kotlin credentials channel implementing AndroidKeyStore wrapping-key + AEAD while ciphertext envelopes stay in Files. credentialAvailable is display-only; every authenticated egress uses request-scoped withCredential and treats request-time invalidation as unavailable. Store seal names the sole Keystore exception. Prevents stack/spine contradiction, alias-only authorization and secret caching across requests.
- (event) spine update finalized: critical findings C1 generational export and C2 credential capability closed; lint, rubric, verification and adversarial gates pass
- (event) Deck regenerated 2026-08-27 to match the final spine: AD cards updated to current text (both-tables seal, one ledger, session-day slot, day-scoped EligibleDay, eight system events, CredentialVault AD-22, generational AD-13, quiet-affordance AD-26); plate 03 gains the Files port and the credentials channel; plate 04 shows the wrapping key + credential envelope and the FR-28 custody row; C6 hole card added for the credential round; stack rows corrected (credential channel, mlkit 32/64-bit, uuid opt-in, API-37 projection); guards recounted at eight with the store seal; foot updated (26 ADs, 19 conventions, five gate rounds).
- (change) binds line updated per PRD 2026-08-27 (UX handoff ledger): '§1.1 principles P1–P6 (and exceptions E1, E2)' -> '(and exception E1)' — E2 removed from the PRD after A-slim genesis dissolved it; engine unaffected (ledger verified energy day-scoping is a superset of the once-a-day check-in UI; energy_set / setting_changed / suggestion_dismissed / report_answered cover every new act)
