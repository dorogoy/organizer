# Spine Pair Review — organizer

## Overall verdict
The 2026-08-26 realignment left the pair mechanically sound: every token reference resolves, every FR citation checked against the PRD is true, all five UJs are present verbatim with climax beats and failure paths, and all six visual artifacts are linked with section-level specificity. The residual debt is concentrated, not scattered: two Dispenser controls introduced by the recent edits (energy control, ambient/self-report suggestions) have behavioural rows but no visual half and no Open Question, dark mode has an orphaned secondary ink, and the voice/EGression additions duplicated FR text in three places. Usable as a downstream contract today; the five medium findings are tightening, not blockers.

## 1. Flow coverage — strong
Extracted the five UJ names from PRD §2.3 and verified each against EXPERIENCE.md Key Flows: named protagonist (Sergio, both roles stated), numbered steps, a bolded climax beat, and a failure/edge path where applicable.

- UJ-1 "Sergio spends a 15-minute pocket on the storage room" — verbatim title; climax = checkpoint-as-permission; failure path = mid-task abandonment. Checkpoint arithmetic (15-min interval in 15-min pocket, close-is-the-offer) matches FR-10 and UJ-1's PRD text.
- UJ-2 "…photographs the trastero and gets a micro-plan" — verbatim; climax = never shown the plan; edge case = Slicer unreachable + consent-declined variant.
- UJ-3 "…returns after six days away" — verbatim; climax = nothing could have accumulated; unauthored Warm Return copy routed to Open Questions (OQ-3). No failure path, correctly — the journey itself is the recovery case.
- UJ-4 "Sergio on a low-battery Sunday" — verbatim; climax = identical celebration; failure path = no network/key. SM-2 self-report woven in with correct persistence semantics.
- UJ-5 "Sergio anota algo a mano en diez segundos" — Spanish title verbatim; climax = capture nowhere to be seen; failure path = recognition unavailable and no-Slicer-irrelevant.

### Findings
No misses — strong.

## 2. Token completeness — strong
Extracted every frontmatter token in DESIGN.md (21 colors, 8 typography roles, 3 radii, 13 spacings, 2 formats, 14 components) and every `{path.to.token}` reference in both files' prose (74 unique paths; the lone `{path.to.token}` in EXPERIENCE Foundation is illustrative syntax, not a reference). All resolve. Every color token carries a hex. Contrast targets stated for all load-bearing combinations: ink-primary on surface-base/raised (15.21:1 / 16.18:1), ink-secondary on surface-base (5.74:1), ink-primary on accent-soft (11.96:1), dark-mode inks (13.93:1 / 12.36:1), plus the icon-mass tier's Δ L* 18–20 threshold.

### Findings
- **medium** `ink-secondary-dark` (#99A0A3) is defined (DESIGN.md:27) and referenced nowhere — and the gap it signals is real: dark mode is declared "in scope and separately authored" (Colors), but the secondary action, support copy and zone-marker line have no dark-mode ink rule anywhere, and OQ-4 covers only destination hues and the missing `icon-mass-ochre` dark counterpart. *Fix:* either specify the dark treatment for `{components.action-secondary}` / `{typography.support}` / `{components.zone-marker}`, or widen DESIGN.md OQ-4 to own it.
- **low** Orphan token `border-strong-dark` (#2A2F31, DESIGN.md:29) — defined, never referenced, and darker than `border-hairline-dark` with no consumer. *Fix:* delete, or name the component that earns it.
- **low** The `formats` frontmatter key (`short-date`, `duration`) is an extension beyond the design-md-spec token table (name/description/colors/typography/rounded/spacing/components). Both files use it consistently and reference it correctly, but consumers running a stock resolver will not know it. *Fix:* one line in DESIGN.md noting `formats` is a project extension of the spec.

## 3. Component coverage — adequate
Extracted every component name from DESIGN.md frontmatter + Components prose and EXPERIENCE.md Component Patterns. All 14 token-named components have a real visual spec in DESIGN.md Components AND a behavioural row in EXPERIENCE.md Component Patterns (icon-glyph / seed-glyph / destination-mark-dark share one EXPERIENCE row whose rule — "drawing specs with no behaviour of their own" — is legitimate). The "Microphone affordance" row references no DESIGN.md component token — and that is acceptable as registered: DESIGN.md OQ-10 owns the Micrófono glyph, says "EXPERIENCE.md already specifies the behaviour… this is the visual half", and EXPERIENCE.md OQ-24 cross-references back. Same pattern holds for the quiet affordance (EXPERIENCE OQ-21 ↔ DESIGN OQ-1). Named, cross-referenced gaps — not inventions needed.

### Findings
- **medium** **Energy control** has a behavioural row (EXPERIENCE.md:136, real rules: 🟢🟡 filter nothing, 🔴 narrows, mid-task semantics) and no visual half anywhere: no DESIGN.md component, no token, no mention, no Open Question. Whether 🟢🟡🔴 are literal emoji or drawn marks is undecidable from the spines — on a surface where "the screen must not add weight" is rule zero. Note the tension with `size-option`'s claim to be "the only selector in the app" (DESIGN.md:123). *Fix:* add a DESIGN.md row or token (and resolve the emoji-or-glyph question), or register it in DESIGN.md Open Questions.
- **medium** **Ambient suggestion** (EXPERIENCE.md:139) and the persistent Sunday self-report (EXPERIENCE.md:47) have no visual treatment in either spine: no card/banner spec, no typography role, no OQ. The self-report is the sharpest case — it *persists until answered* on the Dispenser, the surface where DESIGN.md caps information and mandates "one card, mostly empty space"; a persistent second element there is a layout decision nobody wrote down. *Fix:* spec the ambient container once (it serves three suggestions + the self-report) or register it in DESIGN.md Open Questions.
- **low** **Genesis paths** visual half is inherited rather than specified: photo button, manual-entry field and template rows have no DESIGN.md component; cover is partial via Layout & Spacing's E1/E2 ruling, one Do's & Don'ts row ("under the ordinary card, type and spacing rules", DESIGN.md:524), and EXPERIENCE OQ-6 (row shape). The superseded mockup §4 leaves nothing current. *Fix:* one DESIGN.md paragraph stating "the genesis zone inherits `dispenser-card` / type ramp verbatim; the list rows are `{components.size-option}`-style hairline rows" — or an explicit OQ.

## 4. State coverage — strong
Walked all 17 IA surfaces plus onboarding against the applicable states (empty, cold-load, focus, error, offline, permission-denied). Covered in State Patterns: cold open, warm return, pause/resume, energy, no-Slicer ×7 on one surface, manual capture (+ the confirmed no-error/no-validation absence), checkpoint, offline, person-in-frame, consent-declined, scan wait (exit semantics per FR-16/FR-29), dormant epic, export failure, empty album/dashboard, pool exhausted, later-session-same-day, onboarding-abandoned. Explicitly routed to Open Questions: camera permission denial (OQ-16), empty album/dashboard copy (OQ-12), reward with no Before photo (OQ-15), Decluttering question presentation (OQ-17), onboarding shape (OQ-9), scan-wait affordance (OQ-10), Settings IA (OQ-11), light/dark selection (OQ-14), screen-reader behaviour (OQ-13). Microphone permission refusal is fully specified (affordance absent, keyboard unaffected). The new state rows from the 2026-08-26 pass (pool-exhausted, later-session, onboarding-abandoned, scan-wait) are all present and FR-accurate.

### Findings
No misses — strong.

## 5. Visual reference coverage — strong
Workspace: `mockups/` (color-themes-1, final-verification-1, key-screens-1, seed-at-scale-1, typography-1 — exactly the "five promoted artifacts" both spines claim), `imports/` (dandelion-seed-reference.png), `reconcile-dandelion-seed-reference.md`; no `wireframes/` directory and none referenced. Every file exists and is linked inline with section-level specificity naming what it illustrates: DESIGN links all five mockups (key-screens §§1–5 all cited) plus the import and the reconcile doc; EXPERIENCE links key-screens (§§1, 2, 3, 4, 5), seed-at-scale §4, color-themes-1 and the import. "Spines win on conflict" stated exactly once per file (DESIGN.md:249, EXPERIENCE.md:23) with identical scope (PRD, reference image, mockups, `.working/`). Superseded-content provenance is carried per-artifact ("says so at the top"). No orphans, no unspecific references.

### Findings
No misses — strong.

## 6. Bloat & overspecification — adequate
Judged the deliberate invented sections first: Privacy & Consent Behaviour's 2026-08-26 additions stayed behavioural — the genesis-text bullet states what the screen says and what the send action *is*, the audio bullet is two lines ending on the behavioural consequence ("the egress map gains no destination"); neither re-quotes FR-25/FR-32 mechanics. The restore bullet in Export Silence is the closest to restatement (near-verbatim of FR-30's final consequence) but earns its place by siting import in Settings IA, which OQ-11 consumes. AI Access Path & Degradation is the most duplicative of the three — its first two bullets restate FR-28 consequences (allowlist + verified dates, no free-form endpoint, keystore, Local stub, Managed interface-only) with little behavioural delta beyond the Dispenser/Settings split.

### Findings
- **medium** The dashboard 200% degradation is specified with numbers in four places: DESIGN frontmatter (`reflow`, `known-degradation` on `dashboard-highlight-row`), DESIGN Typography ("~101dp", "four or five lines"), DESIGN Components (`dashboard-highlight-row`), and EXPERIENCE Accessibility Floor (same numbers again). Deliberate load-bearing repetition, but four copies of the same figures is drift bait — the first consumer to change the column count or gap syncs three of them and misses the fourth. *Fix:* keep the full arithmetic in DESIGN Typography, reduce the other three to the rule ("reflows at two caption lines, never shrinks") with a pointer.
- **medium** The microphone-affordance behavioural row (EXPERIENCE.md:144) restates FR-32 clause-by-clause — permission timing, no wake word, no retained audio, origin `manual`, dictation boolean, one-utterance-one-task, nothing-parsed — roughly two-thirds of the row is PRD content; the genuinely behavioural core (transcript lands in the existing field, keyboard never removed, affordance absent when unavailable) is about a third. The Foundation's own rule ("inherited from `sources` by reference and not duplicated here") is the standard the row misses. *Fix:* cut the row to the behavioural delta and cite FR-32 for the rest, as the row already does for OQ-13.
- **low** AI Access Path & Degradation restates FR-28 consequences nearly wholesale (EXPERIENCE.md:240–242). *Fix:* keep the two behavioural lines (allowlist entries state name + verified date in plain Spanish — a copy requirement; configuration never intrudes on the Dispenser) and reference FR-28 for the mechanism.
- **low** The measurement narratives (17-filament correction, Blink fold experiments, ink-parity tables, hero-size measurement) are decision records living in a contract; architecture and story-dev consume their *rules*, not their arithmetic. Acceptable as provenance — both files frame them as records — but they are the bulk of DESIGN.md's length. *Fix:* none required; if the file is ever split, these move to the reconcile layer.

## 7. Inheritance discipline — strong
`sources` frontmatter resolves in both files (prd.md and addendum.md both exist at the cited path; format matches the `{planning_artifacts}` convention). UJ titles verbatim from PRD §2.3, including the Spanish UJ-5. Glossary identity holds: EXPERIENCE uses **Focus Chunk** consistently as *the slot* ("once the day's Focus Chunk has been dealt…", State Patterns "Later session, same day") exactly per PRD §3/ADR-14; the pocket-vs-Time-Bag split is stated correctly (bag 5–30 default 15 bounds the day's total advance, FR-7; the pocket is the declared session length, FR-8) with values matching §10.1. Component names identical across sections in both files; every EXPERIENCE token reference resolves to a DESIGN.md token by name (verified exhaustively). FR-citation audit against the current PRD text — all true: FR-3 pool-exhausted string verbatim; FR-4 energy rules exact (🟢🟡 filter nothing, 🔴 → Instant Habits + ≤ 60 s, mid-task finishes the card); FR-7/FR-8/FR-10 checkpoint arithmetic including the 45/15×3 example and close-is-the-offer; FR-11/FR-31 curation (never creates an Epic, never enumerates entries, onboarding + Settings, abandonment default); FR-25 consent symmetry and genesis send-as-consent as §7's second payload; FR-27 disabled-until-text gate; FR-32 all nine behavioural claims match; SM-2 persistence semantics verbatim; FR-16/FR-29 scan-wait exit (no cap, no timeout, leave/background cancels and discards); FR-23 snowball suppressed at top of range; FR-20/FR-21 labels and blind timer; FR-30 silent export + restore. Both "paid into the PRD" claims check out: §1.1 carries E1/E2 with the exact consequence FRs the spines cite, and §3 + FR-20 now own `Tirar o soltar`. No spine cites an FR for something it does not say.

### Findings
- **low** EXPERIENCE.md:19 says "the thirty FRs" — the PRD carries **32** (FR-1…FR-32, complete). Stale count from before FR-31/FR-32 were added; the same sentence's "six invariant principles" is correct. *Fix:* "the thirty-two FRs" or just "the FRs".

## 8. Shape fit — strong
DESIGN.md body sections in canonical order: Brand & Style → Colors → Typography → Layout & Spacing → Elevation & Depth → Shapes → Components → Do's and Don'ts, with Open Questions appended after (consistent placement; the eight canonical sections themselves omittable-but-order-locked — none omitted, order intact). EXPERIENCE.md required defaults all present: Foundation, Information Architecture, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, Key Flows; Responsive & Platform is dropped with a stated, sound defence (one surface, no breakpoints); the invented sections (Privacy & Consent Behaviour, AI Access Path & Degradation, Export Silence, Inspiration & Anti-patterns) each carry product-defining behaviour no default section would hold — they earn their place. Open Questions numbering contiguous 1–10 (DESIGN) and 1–24 (EXPERIENCE), no gaps, no duplicates. Frontmatter complete on both files (`name`, `description`, `status: final`, `updated: 2026-08-26`, `sources`).

### Findings
No misses — strong.

## Mechanical notes
- Token reference resolution: 74 unique `{path.to.token}` references across both files, all resolving to frontmatter tokens, including sub-paths (`components.seed-glyph.axis`, `components.seed-glyph.motion-dashes`, `components.photo-frame.aspect`, `components.icon-glyph.offset`). Zero broken cross-references after the 34-edit pass.
- Orphan tokens: `border-strong-dark` and `ink-secondary-dark` (see §2). All other 19 colors, all typography roles, all radii, all spacings, both formats and all components are referenced at least once outside their definition.
- Name consistency: the behavioural "Microphone affordance" (EXPERIENCE) and visual "Micrófono glyph" (DESIGN OQ-10 / EXPERIENCE OQ-24) are deliberately distinct names, cross-referenced in both directions — no ambiguity for consumers. "Energy control" and "Ambient suggestion" exist only in EXPERIENCE (see §3).
- Stale count: "thirty FRs" → 32 (see §7).
- Cross-file consistency of the E1/E2 story: DESIGN Layout & Spacing, EXPERIENCE Information Architecture and PRD §1.1 tell the same arithmetic with the same consequence FRs — no drift between the three tellings.
- The `.working/` directory referenced by both spines exists; the mockup count claim ("five promoted artifacts") matches the directory contents exactly.
- `status: final` + `updated: 2026-08-26` on both files, consistent with the edit date under review.
