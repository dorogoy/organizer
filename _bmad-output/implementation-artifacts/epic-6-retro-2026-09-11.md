---
epic: 6
date: 2026-09-11
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 6 Retrospective — Letting Go Without Guilt

## Epic summary

- **Epic:** 6 — Letting Go Without Guilt (FR-19, FR-20, FR-21, FR-22). Stories 6.1–6.7, all `done`; `pending_stories` empty (detect-epic, sprint-status.yaml). Epic re-partitioned at its door 2026-09-09 (5→7 stories, 24 KB story-diet rule; two operationalizing ACs added to new 6.3).
- **Diff range:** `c39cfca^..9b0b3fc` (first story commit `c39cfca` "purge comes first" through last `9b0b3fc` "cumulative declutter metric"). 9 commits, 0 merges.
- **Story attribution** (subjects carry `6.N`, not story slugs — `git_evidence.py stories:[]` empty, attributed manually): 6.1→`c39cfca` · 6.2→`3c9263a` · 6.3→`a709a1e` · 6.4→`1af5c2e` · 6.5→`ed467e6` · 6.6→`6b933ef` · 6.7→`9b0b3fc`. Non-story commits inside the range: `de9b745` (ci: cache v6), `17d9268` (chore: silence JDK 25 native-access warnings). Post-range, non-epic: `5a9bbac` (fix: redact credentials in crash stacks, spec-gh-57 — adjacent hygiene, out of epic scope).
- **Evidence inventory:**
  - Epic spec: `_bmad-output/planning-artifacts/epics.md` §Epic 6 — acceptance criteria **declared** per story.
  - Story specs: all 7 present (`6-1…6-7`; 6.2 as `spec-6-2-…`). Each carries frozen intent, I/O matrix, Design Notes, Review Findings (patched/deferred/rejected with dispositions).
  - Review loops: 6.5 (14 patches), 6.6 (20 patches), 6.7 (14 patches) recorded in-spec; 6.1/6.2/6.4 findings in `deferred-work.md`.
  - `deferred-work.md`: epic-6-era deferrals from 6-1, 6-2, 6-4, 6-5, 6-7 reviews; two epic-5 deferrals **resolved** by 6-6 (`_stripAct` extraction; 5.12 dismissal-arm coverage).
  - Previous retro: `epic-5-retro-2026-09-09.md`; sprint-status action items — epic-5 items 9–18 all `done`; epic-4 items 5 (F5 image seam) and 6 (F6 hardening backlog) still `open`.
  - Session logs: build-session conversation records not available to this run → process-lesson analysis from logs is **narrowed** to what the artifacts record.
  - Manual verification: only 6.1 records an emulator pass (found & fixed a real FR-19 defect on the completion path). 6.2–6.7 list "Manual checks (if no CLI)" with no recorded run.
- **Volume (non-merge, from `git_evidence.py`):** ~10.4k added / ~1.3k deleted across 80 paths; largest: dispenser_screen_test +1020/−78, dispenser_controller_test +1063/−22, substrate_test +699, decluttering_protocol_screen +490/−118, log_test +544, destination_flow_screen_test +633, log_entry.dart +426/−46, dispenser_screen.dart +356/−287, destination_flow_screen.dart +290/−13, weave.dart +245/−57.

## Findings

### Aggregate views

- **Architecture delta — checked and clean.** Core purity intact (no `package:flutter` import under `packages/core/`; `tool/check_core_purity.dart` green inside the gate). AD-20 holds: purge entered as a `CandidatePrecedence` member + `purgeCandidates()` in `packages/core/lib/weave/weave.dart:534` — `core/weave` remains the only deal emitter; shell callers consume `nextDeal`/`composeDay`. New derive `packages/core/lib/derive/quarantine.dart` (91 lines) sits with its derive siblings. Schema grew additively v12→v13 (`lib/store/substrate.drift` triage columns + `triage_box_id`), no CHECK/FK per AD-23. Disposition: **accept**.
- **God-class growth — watch, not defect.** `packages/core/lib/log/log_entry.dart` 1367→1747 lines (+28% in one epic; the whole 24-kind vocabulary lives in one file, census-pinned by `no_lateness_proof_test`); `lib/dispenser/dispenser_controller.dart` 1157→1362 (+18%); `packages/core/lib/weave/weave.dart` 1210→1398 (+15%); `lib/ui/dispenser/dispenser_screen.dart` high churn but net +69 (1385→1454) — refactor absorbed the growth. All four carry structural pins. Disposition: **accept**, re-measure at the Epic 7 door (dashboard kinds and surfaces will press the same files).
- **Duplication map — net improved.** The epic *removed* duplication it inherited: `_stripAct` extraction (story 6.6, resolving the 5.13 deferral in `deferred-work.md`) and `_runCompletionAct` (6.5 review, `lib/ui/dispenser/dispenser_screen.dart:647`) consolidated two write-path families. The 2-3-era hand-copies (`_onDone`/`_onSkip`/`_onDeclarePocket`/`_onPause`) remain as already recorded — not re-flagged. Disposition: **accept**.
- **Pattern divergence — checked and clean.** New kinds follow the past-tense `snake_case` + item-act shapes (`epic_activated` precedent); unknown triage wire names surface as `LogRecordFlaw` at the read boundary (`packages/core/test/triage_commands_test.dart`); ARB keys flat with `@` blocks (string-table audit green). Disposition: **accept**.
- **Spec-to-implementation reconciliation:**
  - *Accepted deviation (ruled, verify-then-record):* 6.6's AC reads "a dismissed follow-up … **it never returns for that box**"; the frozen 6.6 matrix operationalizes it as "process death after dismiss → re-offer within that day only; never from the next day on" (spec `_bmad-output/implementation-artifacts/6-6-the-blind-six-month-follow-up.md`, matrix row "Process death after dismiss"). Verified on device: same-day relaunch re-offers; dismissal writes nothing; the window is derived (`==` due day) and closes next day. Recommend reconciling the epics.md AC wording so a future audit does not re-flag it. Disposition: **spec reconciliation proposed**.
  - *Accepted deviation (sanctioned reword):* purge authored copy shipped as "Elegir un objeto de la estancia y decidir qué hacer con él" (`lib/l10n/app_es.arb`) against the spec's proposed "Elegir un objeto del espacio…" — the 6.1 Ask First explicitly allowed rewording at review. Disposition: **accept**.
  - *Expected gap, not a defect:* 6.7's metric has **no runtime surface** — correct per its AC (figures cross only to Epic 7's FR-23 dashboard); `liberatedVolume` ("≈ 3 cajas liberadas") sits in the ARB with no consumer. Disposition: **accept** (Epic 7 consumes it).
  - *Verification gap (process):* stories 6.2–6.7 recorded no manual emulator pass (only 6.1 did); this retro's behavior check below covers the epic-wide walk. Disposition: **fix now** — one recorded emulator pass over the whole flow before Epic 7 opens (action item).

### Diff-scope review (bmad-review lenses, cross-story)

Reviewer agent over `c39cfca^..9b0b3fc` (adversarial, edge-case, verification-gap; per-story review-loop findings in the specs and `deferred-work.md` treated as known). Mechanical seals re-verified in the same pass: `make check` end-to-end green, core `dart test` 841 pass. Both findings below re-checked against the primary sources by this retro (dispenser_screen.dart:620-621; dispenser_controller.dart:646-651, 690-699; triage_commands.dart:55-58; dispenser_controller_test.dart:5136-5153). Everything else hunted at the boundaries came back clean — the full checked-and-clean list lives in the agent transcript; the load-bearing items: purge closure via terminal acts on `purge:{stableId}`, ladder/chunk precedence with the new routes, `item_triaged` wire consistency minter→read-boundary→fold (25-kind census), box-fold ↔ follow-up day/offset conventions (DST-safe), one-minted-instant completion act with pre-minted box id and staleness-before-append, AD-20 (no shell site composes a deal), AD-1/schema (no quarantine table, no stored follow-up date, idempotent v11→v13 chain), warm-return contact-only classification, all shell record copiers carry the new triage columns.

- **[low] The quarantine tap silently discards the volume tag the same visit just collected.** `onQuarantine` captures `dealt` alone while `onDestination` threads `volumeTag` (`lib/ui/dispenser/dispenser_screen.dart:620-621`); `quarantineAndComplete` takes no tag (`lib/dispenser/dispenser_controller.dart:690-699`) and the minter forbids a tag beside quarantine by construction ("a quarantined object liberates nothing", `packages/core/lib/commands/triage_commands.dart:55-58`). The log stays honest; the surface voids the user's just-given answer with no signal. Per-story reviews each pinned their own surface; the discarded-input UX sits at the 6.3×6.5 seam. Disposition: **defer to a product ruling** (accept as "hesitation liberates nothing" or renegotiate the flow order) — Sergio's call.
- **[low] "Figures come only from taps during purge" (FR-22) is enforced by call-graph shape alone.** `triageAndComplete` asserts only `destination != quarantine` (`lib/dispenser/dispenser_controller.dart:646-651`); `quarantineAndComplete` asserts nothing about the dealt card; no pin fails if either is handed an ordinary dealt card (`test/dispenser/dispenser_controller_test.dart:5136-5153` pins only the destination assert). Today the only entry is the purge protocol route, so the rule holds by wiring. One assert (`dealt.card.id` starts with the purge prefix) + one pin closes it — the same shape 6.1's review used for its deferred `required` sweep. Disposition: **fix now** (small remediation, next dev pass).

## Behavior verification

Emulator pass, 2026-09-11, Android 36 `organizer36` (KVM), fresh `make build` of HEAD `5a9bbac`^ = `9b0b3fc`, two seeded worlds (pool-fact groups + `epic_activated` rows via sqlite; app stopped during seed; permissions restored). Gate on HEAD green before the walk: `make check` + `flutter test` (1208 passed) + `dart format` + `flutter analyze`. Evidence: screenshots `/tmp/s1–s17.png` (session), sqlite pulls `/tmp/log*.sqlite`.

- **6.1 (twice):** seeded active epic → the dealt card was the purge card — authored text verbatim, `1 min` chip, ordinary furniture, unsplit secondary; no styling or announcement. `Hecho` opened the protocol frame (the only entry). Completing a standing non-purge card bundled the purge deal at the same instant (`card_done` → `card_dealt(purge:seed-fact-1)`), and the organization step dealt only after the purge answered. FR-19/UX-DR31 hold on device.
- **6.2:** both questions verbatim, only `Sí`/`No` each, no skip, no preselection; answers revisable (tinted selected state); no log write on answers.
- **6.3:** the volume block appeared only after both answers — exactly `Bolsa`/`Caja`/`Caja grande`/`Mueble` + `Sin etiqueta`, equal chips, no numeric input. Declining wrote no tag (log row carries destination only).
- **6.4:** full-screen trio, labels verbatim, hues only inside the glyphs, silhouettes distinct, no tile/default/order signal. Tap fired the ordered act at one instant: `item_triaged(trash_recycle)` → `card_done(purge:seed-fact-1)` → `card_dealt(seed-fact-1)`.
- **6.5:** `Todavía no lo decido` (quiet-text affordance) → ordered act: `box_created` → `item_triaged(quarantine, triage_box_id = the box_created id)` → `card_done(purge:seed-fact-b1)` → `card_dealt(seed-fact-b1)`. Schema v13 confirms no quarantine table — box membership exists only as the link.
- **6.6:** device clock set to box day + 6 months → the strip resident appeared with the approved copy ("Hace seis meses fechaste una caja. Hoy es un buen día para donarla."); ✕ dismissal → **zero log writes** (33 rows before and after); same-day relaunch re-offered (the frozen matrix's accepted process-death shape); the derived window closes from the next day.
- **6.7:** no runtime surface by design; the derivation ran inside the green core suite (monotonicity, order-independence, unknown-kind tolerance pinned).
- *Incidental honest-functioning checks (passed):* the rescue path without a stored key surfaced the honest no-key screen with the `Anotarlo` exit (4.5's register, not a quiet close); the store's insert-only trigger refused a raw SQL `UPDATE` from outside the app (AD-2 enforced at schema level).

## Previous-retro follow-through

Prior retro: `epic-5-retro-2026-09-09.md`; its action items (sprint-status `epic-5-retro-item-9…18`) are all recorded `done` — no transitions to propose. Landing evidence seen from this run's sources: the scan-honesty train (`c70f53c`, between the retro and this epic's first commit) covers the F-S2/F-S1 rulings; story 6.6 additionally **resolved two epic-5-era deferrals** recorded in `deferred-work.md` — the 5.12 untested check-in dismissal arm (covered by the `_FailReadWhileArmedStore` ✕ test) and the 5.13 `_stripAct` extraction (now the shared seam routing `_onSetEnergy`, both dismissals, both suggestion paths and `_onDismissQuarantineFollowUp`).

Still `open` from **epic 4** (carried at this epic's door, no evidence of landing inside the epic-6 range, no transition proposed):

- `epic-4-retro-item-5-epic-4-f5-epic-5-pre-work-image-seam-fix` — the seam sealing itself landed with 5.3; the byte-size-guard sub-item shows no commit in `c39cfca^..9b0b3fc` touching it.
- `epic-4-retro-item-6-epic-4-f6-hardening-backlog-background-e` — background executor for the Keystore channel: no evidence in the range.

## Action items

Proposed, not applied — each awaits its human owner's go:

1. **[fix now]** Add the act-level guard for FR-22: `triageAndComplete`/`quarantineAndComplete` assert the dealt card is a purge card (`dealt.card.id` starts with `purge:`), plus one controller pin handing an ordinary dealt card and expecting the throw. Owner: next dev pass touching `DispenserController` (Epic 7 pre-work candidate). Source: diff-scope finding 2.
2. **[product ruling]** The hesitation outcome silently voids the tag collected in the same visit — accept ("hesitation liberates nothing", the log already says so) or renegotiate the flow (tag block after the destination, or suppress it when hesitation is offered). Owner: Sergio. Source: diff-scope finding 1.
3. **[spec reconciliation]** Reconcile epics.md 6.6's AC wording ("it never returns for that box") to the frozen 6.6 matrix's operationalization ("re-offer within that day only; never from the next day on") so future audits stop re-flagging the observed-and-ruled shape. Owner: Sergio (human applies to the project contract). Source: aggregate reconciliation + device evidence.
4. **[process]** One recorded emulator walk at epic close, before the retrospective, as standing policy — 6.2–6.7 shipped without one (only 6.1 recorded a pass; its walk found a real FR-19 defect). This retro performed the walk clean, which is the evidence the practice is cheap. Owner: Sergio decides whether it becomes standing policy (project-context line).
5. **[process, standing]** Re-partition each epic at its door under the 24 KB story-diet gate — the practice held for epic 6 (the 5→7 re-partition of 2026-09-09 produced seven specs of 10,5–19,8 KB, all under budget; no story exhausted its session). Apply the same gate at the epic 7 door before its first story. Owner: Sergio (already recorded in `project-context.md` → Story size budget; this item tracks its next application).

Process lesson (upstream, no item): epic-level rules ("figures come only from purge taps") have no single-story owner at review time — the retro is where they get their guard; the re-partition that split 6.3/6.5 across one surface also split the surface's contract across two frozen matrices.

## Acceptance verdict

**accepted-with-open-items** — criteria **declared** (epics.md §Epic 6, per-story ACs).

- All 7 stories `done`; `pending_stories` empty (`detect-epic --epic 6`).
- Every declared AC verified: on device for the runtime ACs (purge precedence + ordinary card + sole entry; question copy/choices; tag vocabulary and decline-writes-nothing; trio equality, verbatim labels, hue-in-glyph, silhouettes; box linkage and log-derived reconstruction; follow-up due-at-+6-months, copy-on-date-alone, zero-write dismissal) and by the pinned suites where device observation is impractical (dark mode, 200% scaling, seed-glyph geometry, metric fold properties). Gate green on HEAD: `make check`, `flutter test` (1208), format, analyze. The story-diet held: all seven specs 10,5–19,8 KB (`wc -c`), none over the 24 KB gate.
- No blocking findings. Open items: action items 1–3 above (one small guard, one product ruling, one AC reconciliation) — all tracked, none breaks a declared AC.
- Machine verdict rendered on the evidence; human override available per the standing rule.

## Open questions

_(Phase 5 — pending)_
