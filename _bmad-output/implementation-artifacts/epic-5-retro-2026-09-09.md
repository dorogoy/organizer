---
epic: 5
date: 2026-09-09
verdict: accepted-with-open-items
criteria: declared
headless: false
phase: complete
---

# Epic 5 retrospective — From a Personal Project to Its First Card

## Epic summary

Requested by Sergio (`/bmad-retrospective`); epic 5 confirmed interactively. Sprint mode. `detect-epic --epic 5` reports `story_count: 13`, `pending_stories: []`, `retro_key: epic-5-retrospective`, `retro_status: optional`. Sprint-status `epic-5` is still `in-progress` (all stories `done`; epic key not yet flipped).

**Diff range:** `95dc9dd..7305f99` (`9324d5a^..HEAD`). Left endpoint is the parent of the first story commit so story 5.1 is included. Wagon 0 (`95dc9dd`, pre-epic Dispenser view-arm split) is excluded. HEAD: `7305f99`. Fourteen squash-style single-parent commits (GitHub `#33`–`#46`); `git_evidence.py` reports `merge_count: 0`, `merges_measured: 0`, `binary_revisions: 0`.

**Stories completed (13/13, all `done` in sprint-status and in story-file frontmatter):**

| Story | Title | Commit on range |
|---|---|---|
| 5.1 | The on-device face gate, verified before it is trusted | `9324d5a` (#33) |
| 5.2 | The camera entry, and shooting the frame | `546c79e` (#34) |
| 5.3 | The image seam, sealed before a payload ships | `55f209a` (#35) |
| 5.4 | The scan cache and its single-use consent token | `e215903` (#36) |
| 5.5 | The consent gate | `d82592e` (#37) |
| 5.6 | The unbounded wait and honest abandonment | `306eb7d` (#38) |
| 5.7 | The slice lands as steps | `8b7f159` (#39) |
| 5.8 | Typed genesis — Analizar | `8ad91b7` (#40) |
| 5.9 | Epic material in the weave | `aad6316` (#41) |
| 5.10 | Invisible buffers | `c0d8195` (#42) |
| 5.11 | The curation-row and its Settings home | `4cb53f1` (#43) |
| 5.12 | Curation's other two homes — the E1 surface and the one-time strip | `0c139de` (#44) |
| 5.13 | The gentle seasonal suggestion | `4242f0e` (#45) + pin `7305f99` (#46) |

Unfinished stories accepted over: none. Phase 3 team discussion: skipped (opt-in; not requested).

### Evidence inventory

**Available**

- Epic spec: `_bmad-output/planning-artifacts/epics.md` § Epic 5 (from line 1820). Declared FRs: FR-11 (Epic half), FR-13, FR-15, FR-16, FR-25, FR-31 (curation half). Per-story acceptance criteria declared in the same file. Re-partition 7→13 recorded 2026-09-05.
- Epic context: `_bmad-output/implementation-artifacts/epic-5-context.md`.
- Story files: thirteen specs under `_bmad-output/implementation-artifacts/5-*.md`, each `status: done`. No remaining unchecked review boxes.
- Architecture / PRD: `_bmad-output/planning-artifacts/architecture/architecture-organizer-2026-08-26/ARCHITECTURE-SPINE.md` (not rewritten on this range; `updated: 2026-08-27`); PRD under `planning-artifacts/prds/`.
- Diff + commits: `git_evidence.py` over `95dc9dd..7305f99`. 14 commits, 173 files, +33231 / −1742 / net +31489 (production `lib/` + `packages/core/lib/` + `android/` + `ios/`: 75 files, +9164 / −942 / net +8222). Every supplied story id `5.1`–`5.13` attributes to at least one commit.
- Sprint status: `_bmad-output/implementation-artifacts/sprint-status.yaml`.
- Previous retro: `_bmad-output/implementation-artifacts/epic-4-retro-2026-09-05.md` (`verdict: accepted-with-open-items`).
- Choir protocol: `_bmad-output/implementation-artifacts/choir-pilot-protocol.md` — scoreboard rows blank.
- Deferred-work ledger: `_bmad-output/implementation-artifacts/deferred-work.md`.
- Story completion / review records inside each story file.

**Missing**

- Full conversation/session logs for the thirteen stories: **not found**. Process-lesson analysis is narrowed to story-file records, `deferred-work.md`, the choir protocol, and sprint-status — not "why a session turned."
- Choir measurement numbers: scoreboard empty; `epic-5-choir-pilot-run-and-measure` still `open`. Epic 6 mode has **no measured credits**.
- Cold 24 KB presentation sizes: current story files include review/completion text and several exceed 24 KB (`wc -c` now: 5.6=34043, 5.2=30699, 5.5=29832, 5.13=28423, 5.4=28176, 5.3=27049, 5.12=26677, 5.11=24908). The gate is "measured cold at review presentation"; those cold bytes are not archived. Cannot claim a gate miss or a pass from current files.

### Change-volume ranking (candidates inspected in Phase 2)

Production (net lines, from `files`; this range has no merges):

| net | commits | path | HEAD size |
|---:|---:|---|---:|
| +847 | 7 | `lib/scan/scan_controller.dart` | 847 |
| +584 | 2 | `lib/ui/settings/nuevo_proyecto_screen.dart` | 644 |
| +466 | 3 | `lib/genesis/genesis_controller.dart` | 466 |
| +427 | 2 | `lib/ui/scan/scan_screen.dart` | 427 |
| +423 | 3 | `lib/ui/scan/consent_gate_screen.dart` | 423 |
| +308 | 4 | `packages/core/lib/weave/weave.dart` | 1210 |
| +283 | 4 | `lib/ui/dispenser/dispenser_screen.dart` | 1385 (1529 at epic-4 F7; wagon 0 split is outside this range) |
| +256 | 8 | `packages/core/lib/log/log_entry.dart` | — |
| +194 | 6 | `lib/dispenser/dispenser_controller.dart` | 1157 |

Highest-churn tests: `packages/core/test/weave_test.dart` +1608 (now 5710); `test/scan/scan_controller_test.dart` +1506 (now 1506); `test/ui/dispenser/dispenser_screen_test.dart` +1090 (now 6510).

## Findings

Every finding was re-checked against HEAD before routing. Sub-agent reports that did not hold (ScanConsent-as-secret; `malformedInput→unreachable` as a defect; weekly-curation delay as a homes/Dispenser disagreement; 2-minute stall bound as a UX timeout) were dropped, not routed.

### Aggregate — architecture delta

New shell modules this epic: `lib/plugins/{camera,mlkit_face}/`, `lib/scan/`, `lib/ui/scan/`, `lib/genesis/`, `packages/core` ports `face_gate_port` + `scan_consent`, scan/curation/suggestion commands, `CameraChannel.kt`. Runtime deps added: `google_mlkit_face_detection 0.15.1`, `camera 0.12.0+2` (`pubspec.yaml:37–53`, commit `546c79e`). Core stays Dart-pure (no `package:flutter` under `packages/core/lib`). Plugin packages confined to their shells. The `weave.dart` ↔ `eligible_day.dart` import cycle **pre-dates** Epic 5.

**F-A1 — AD-11 spine still says three channels after the fourth shipped.** Spine `ARCHITECTURE-SPINE.md:110–113` ("Three channels… Camera… remain plugin-served"). As-built: `tool/check_wire_contracts.dart:21–28` "the build decides four"; `MainActivity.kt:6–30` constructs dictate, credentials, **camera**. Capture remains plugin-served (`plugin_camera_shell.dart:24`); permission is a hand-written channel (`CameraChannel.kt:8–16`). Already deferred in the 5.2 story (`5-2-…md:234`). *Instance: defer (spec reconciliation). Lesson: a channel story must edit the spine in the same commit as the census.*

**F-A2 — Origin tagging names `LocalSlicer` outside `lib/egress/`.** `scan_controller.dart:17,684` and `genesis_controller.dart:41,227` (`slicer is LocalSlicer ? Origin.local : Origin.cloud`). Spine AD-9 (`ARCHITECTURE-SPINE.md:102`) says adding Local/Managed "changes no call site outside `lib/egress/`". *Instance: defer. Lesson: origin belongs on the port outcome, not an `is` check.*

**F-A3 — Stale face-gate comment still promises a 5.5 reopen.** `packages/core/lib/ports/face_gate_port.dart:14–15` ("composition … reopens before 5.5 ships the first payload"). 5.5 P1 moved that reopen to this retro (`5-5-the-consent-gate.md:18,106`). *Instance: fix now (comment only).*

### Aggregate — duplication / size / pattern

**F-D1 — Scan vs genesis landing/epoch/failure writers diverged (story boundary 5.7↔5.8↔5.9).** Same contract, two implementations. Genesis re-checks `_epoch` in the queue body (`genesis_controller.dart:379,388,407`) and after landing (`:230–232`). Scan's `_appendScanLanded` (`scan_controller.dart:771–824`) and `_appendScanSliceFailed` (`:740–748`) do **not**. Scan's `grantConsent` clears `_sliceInFlight` then lands (`:655–686`) and returns `ScanConsentDelivered` even though landing uses `.catchError((Object _) {})` (`:823–824`). Deferred-work.md already named the epoch split (~305–308). Verification-gap: `genesis_controller_test.dart:584–619` pins the queued-landing close; scan's `scan_controller_test.dart:1109–1141` pins the opposite race ("landing completes unconditionally"). *Instance: fix now. Lesson: a second channel that copies a first must copy the safety property, or share one landing helper.*

**F-D2 — Dispenser write-path family still unrolled (5.13 added the fourth recovery-read copy).** `_seasonalSuggestionWrite` (`dispenser_screen.dart:1007–1056`) comments " `_onAnswerReport`'s mechanics verbatim" beside `_onSetEnergy` (`:783–830`). Skip/done/dismiss remain a second family (empty-frame, no recovery read). Already in deferred-work.md ~313–314. *Instance: defer (touches every resident). Lesson: the next strip resident must not copy a fifth time — extract or split the story.*

**F-D3 — God-class check.** `dispenser_screen.dart` is 1385 lines (down from 1529 after wagon 0, then grew 5.12/5.13 handlers); two types; view arms already split. Collision file, not a mixed-domain god-class. `scan_controller.dart` 847 is one channel stacked 5.2–5.7 — cohesive. `weave.dart` 1210 is a library of functions (AD-20 one resolver), not a class. `dispenser_screen_test.dart` 6510 / `weave_test.dart` 5710 are accumulated suites. *Instance: accept as-is (size), with F-D2 as the live duplication. Lesson: wagon 0 paid off; the remaining gravity is write-path copies, not view arms.*

**F-D4 — `writeScanCappedCopy` has no production caller.** Port still claims frame + capped copy (`files_port.dart:55–85`; `app_files.dart:28–34,321`). Cap is in-memory (`egress_dispatch.dart:80–91`). Grep of `lib/**/*.dart` finds only the adapter definition. *Instance: defer (spec letter vs as-built). Lesson: do not add FilesPort methods a story will not call.*

### Aggregate — spec-to-implementation

Declared ACs in `epics.md:1820–2253`. Clusters 2, 5, 6, 7, 8, 9, 10, 11 **VERIFIED** at HEAD (consent pair, 3–5 min tags + originContext, Analizar + no template Epics, LRS + append-order + supersession conjunct, invisible buffers, three curation homes + weekly delay, seasonal ≤1/season, camera ask at first scan). Binding spec-change renegotiations: 5.1 4 FN/12; 5.3 12.5 MP ceiling; 5.5 P1 composition banked; 5.9 supersession conjunct + append-order tie-break; 5.13 accept activates, dismissal scopes to tap season.

**F-S1 — Face-gate composition never reopened before the first payload.** Production still face-only (`mlkit_face_gate.dart:35–60`, `minFaceSize: 0.0`). Residual **4 FN / 12** accepted in 5.1's builder deferral; 5.5 P1 kept it banked and moved the reopen **to this retro** (`5-5-the-consent-gate.md:18,106`; `epic-5-context.md:28`). The original zero-FN bar (`epics.md:1851–1853`) does not hold. **Party 2026-09-09, Sergio:** the gate is worth having and worth *telling* when it fires; it is **not** indispensable and gets **no** product weight. Composition / extra ML packs that bloat the APK are rejected as bloatware. Load-bearing send control is the BYOK token + per-scan consent (“aceptar el token de un tercero ya implica la cláusula de envío”). Written into `project-context.md`. *Instance: accept face-only as shipped; do not reopen. Lesson: a courtesy control must not be allowed to set APK size or story priority.*

**F-S2 — Remaining quiet-close rows still hide shoot-side system failures.** Seeded for this retro (`epic-5-context.md:67–69`; `5-2-…md:236–242`; `deferred-work.md:280–283`). Communicate: open interrupted/unavailable and shutter `CameraShotAccessLost` → `scanOpenFailed` (`scan_screen.dart:264–277,341–368`). Still quiet at HEAD: `CameraShotNone` and detector throw → `ScanShootClosed` pop, no row (`scan_controller.dart:329–369`; `scan_screen.dart:262–263`). 5.5 P3 / 5.6 Never left this here. **Party 2026-09-09, Sergio:** functioning failures are told to the user; this is **not** anti-frustration. Anti-frustration is the spirit of the household tasks (no nag, no "¿seguimos?"). Hiding an error that blocks the app or changes the result the user perceives is a different (forbidden) thing. Detector-error-past-retry **and** missed shot (`CameraShotNone`) join the honest notice (`scanOpenFailed` path), never `permission_refused` / `face_refused`, never a quiet pop that reads as success. Written into `project-context.md`. *Instance: fix now (whole functioning-error matrix still quiet at HEAD). Lesson: two registers — task spirit vs product honesty — must not share a quiet-close implementation.*

**F-S3 — 5.6 "leave cancels" does not cancel the wire.** Wait UI is uncapped (`consent_gate_screen.dart:18–23`). Close mints `scan_abandoned` and unlinks (`scan_controller.dart:407–433`) but never completes `byok_wire.dart`'s `abortTrigger` (armed only on `wireSendTimeout`, `:280–338`). Consent is consumed before the cap (`egress_dispatch.dart:68–91`). The photo can still leave the device after the user left the wait. *Instance: fix now (privacy / AD-8). Lesson: abandonment is not honest if only the local cache dies.*

Accepted deviations (do not re-flag):

- `malformedInput` folds to the provider-unresponsive string — **declared** 5.3 AC2 (`no_slicer_cause.dart:69–73`).
- Weekly cluster switches bind immediately on the row and take effect at the next week boundary — **declared** 5.11 (`curation_test.dart:226`).
- `wireSendTimeout = Duration(minutes: 2)` is a stall bound, not a wait-surface timeout (`byok_wire.dart:280–286`); 5.6 Never forbade touching egress.
- `ScanConsent` is a compile-time capability, not a secret; "never reconstructible" in `scan_consent.dart:26–30` is **from the log**.
- Genesis reuses `consent_granted` / `scan_abandoned` kinds with no cache/token — **declared** 5.8.
- Per-suite `_RecordingStore` fixtures — epic-4 F7 accepted.

### Diff-scope review (bmad-review)

Lenses: adversarial, edge-case-hunter, verification-gap over `95dc9dd..7305f99`, weighted at story boundaries. Structure/prose skipped (content class: code). Findings below were verified at HEAD; padded or already-closed items dropped.

**F-R1 — Background during an in-flight shoot skips close.** `scan_screen.dart:144` (`if (!_openInFlight && !_shooting && _granted)`). 5.6 AC: backgrounding cancels. The shutter path keeps the lens; resume may re-open beside a crash-backstop sweep (`session_controller.dart:196–214`). Edge-case lens: sweep can delete a standing frame under an in-flight gate. *Instance: fix now.*

**F-R2 — Kotlin camera permission machine has no executable test.** `CameraChannel.kt:92–100`; `android/app/src` has only debug/main/profile. Dart pins mock the messenger (`test/plugins/camera_channel_test.dart:22–84`). Extends epic-4 F4's androidTest half (already open). *Instance: defer (same missing infra). Lesson: every new Kotlin channel inherits F4 until androidTest exists.*

**F-R3 — Quiet-close matrix untested at the widget.** Controller pins `ScanShootClosed` (`scan_controller_test.dart:539–573`); `scan_screen_test.dart` never drives `CameraShotNone` or a throwing gate into the pop (AccessLost and `throwOnShoot` only). *Instance: fix now for both remaining arms (widget pins ride the F-S2 ruling).*

**F-R4 — Unlink-on-every-terminal-path unasserted on unavailable-open and AccessLost.** Interrupted pins unlink (`scan_controller_test.dart:361–382`); unavailable (`:390–402`) and AccessLost (`:637–660`) do not. *Instance: defer.*

**F-R5 — Slack invisibility widget pin is a census *diff*, not an absence.** `dispenser_screen_test.dart:3655–3950` asserts gap−control is the greeting wrap; a remaining-count chip on *both* screens would pass. Source scan in `no_lateness_proof_test.dart:195–310` is lex-based. Production `epicBufferedTargets` (`weave.dart:533–593`) is unused by the deal pipeline (`:565–568`) — slack chrome is still not on a surface. *Instance: defer (broken verification, not a live surface defect).*

**F-R6 — `ResolutionPreset.max` vs 12.5 MP ceiling still live.** `plugin_camera_shell.dart:117`; `egressPixelCeiling = 12_500_000` (`image_cap.dart:93`). 12 MP class survives (`image_cap_test.dart`); over-ceiling rejects after consent is burned. Already in deferred-work.md (5.2/5.3). *Instance: defer (existing ledger). Do not duplicate as a new sprint item.*

### Process (no session logs)

**F-P1 — Choir pilot was not measured.** Protocol scoreboard (`choir-pilot-protocol.md:72–79`) empty for pairs A/B/C. Wagon 0 warm-up reports exist under `_bmad-output/implementation-artifacts/wagon0/` and are excluded from the comparison by the protocol. **Party 2026-09-09, Sergio: "ciérralo, no se corrió."** Item `epic-5-choir-pilot-run-and-measure` → `done` as not-run. Epic 6's working mode is decided at its door **without** a choir scoreboard and without inventing credits. *Instance: accept (process). Lesson: a pilot without a filled scoreboard is not a pilot — close it, don't carry it as fake debt.*

**F-P2 — 24 KB diet cannot be audited after the fact.** Current spec files grew with review patches; several exceed 24 KB. Cold presentation bytes were not kept. *Instance: accept the gap; next epic archive `wc -c` at review presentation in the story file. Lesson: a numeric gate needs a numeric snapshot.*

**F-P3 — Story split worked; standing practice from here.** Party 2026-09-09, Sergio: dividing the work (Epic 5 re-partition 7→13, 2026-09-05 correct-course, `project-context.md` 24 KB gate) *funcionó muy bien*. Clarified same session: **each epic is re-partitioned at its door**, not a bulk re-split of 6–9 tonight (John's 2026-09-05 just-in-time win, reaffirmed). Evidence: thirteen stories `done` vs the 4-6 session that exhausted the machine (spec 32 KB, memlog 2026-09-05). Distinct from the choir: the diet is the law; the choir only distributes parts — and the choir was not measured. *Instance: accept as process win. Lesson: `lo pequeño sigue mandando` is no longer a pilot hypothesis.*

## Behavior verification

Exercised this run, inside devbox, at HEAD `7305f99`:

- **`make gate` (NFR17):** `flutter test` **1111 tests, all passed** (`00:34 +1111: All tests passed!`); `dart format --set-exit-if-changed .` green (no "Changed" in the log); `flutter analyze` — **No issues found** (6.4s). Log: session terminal `call-63c739f4-734b-42ec-9109-93de00892e93-58.log`.
- **`make check`:** **green** (2026-09-09 17:55, exit 0). Core purity, no-literal-strings, text-scaling, string-table audit (183/183), forbidden vocabulary, store seal, catalogue floor/id-diff/evolution, wire contracts, export redaction, egress imports, Gradle graph (3 configs / 309 entries), Android manifest (3 variants), eval harness **94 tests passed**, codegen-check passed.
- **Runtime on a handset/emulator:** **not exercised this run.** Passing suites do not substitute for shooting a frame, granting consent, or typing Analizar on a device. Story files record some in-story manual passes (e.g. 5.13 first-opening on a seeded dormant Epic); those were not re-run here. Narrowed: camera permission, face-gate on real pixels, wait/abandon, and seasonal strip chrome are **never checked** at runtime by this retro.

## Previous-retro follow-through

Prior document: `epic-4-retro-2026-09-05.md`. Sprint-status `action_items` exist. Selectors below are the exact `id` strings.

- **`epic-4-retro-item-1-epic-4-f1-guard-the-rescue-success-branc`** (yaml: `done`): landed. Success arm clears only the flight's markers (`dispenser_screen.dart:469–483`); pin `dispenser_screen_test.dart:2557–2624`. `retro_concurrent_rescue_test.dart` is gone. **Proposed status: done** (already `done`; no write).
- **`epic-4-retro-item-2-epic-4-f2-funnel-non-answer-write-path-c`** (yaml: `done`): landed. Non-answer writes go through `_commitView` (`:642–651`). **Proposed: done** (already).
- **`epic-4-retro-item-3-epic-4-f3-bound-rescue-step-text-length`** (yaml: `done`): landed. `rescueStepTextMost = 120` (`rescue_steps.dart:55,119–121`); pin `rescue_steps_test.dart:155–199`. **Proposed: done** (already).
- **`epic-4-retro-item-4-epic-4-f4-add-an-android-debug-build-ste`** (yaml: `open`): **split evidence.** CI `make build` landed (`.github/workflows/ci.yml:45–46`; spec `spec-epic4-f4-ci-android-build.md` `done`). androidTest half: **no evidence found** — no `androidTest` source set under `android/`. **Proposed status: keep `open`** (the item's second half is the remainder). No `--set-action-status` unless Sergio confirms a split/close.
- **`epic-4-retro-item-5-epic-4-f5-epic-5-pre-work-image-seam-fix`** (yaml: `done`): landed as 5.3 `55f209a`. Shared sniff `egressImageFormatOf` (`image_cap.dart:16–28`); undeclarable mime throws `MalformedImageInput` (`byok_wire.dart:274–278`); pre-transport decode → `malformedInput` (`byok_slicer.dart:182–186`); ceiling 12.5 MP (`image_cap.dart:93`). **Proposed: done** (already).
- **`epic-4-retro-item-6-epic-4-f6-hardening-backlog-background-e`** (yaml: `open`): **no evidence found** for any of the four legs. Keystore still main-looper (`CredentialsChannel.kt:29–31,:43`); `byok_slicer.dart:61` still `on Object`; vault still seals empty plaintext (`credential_vault.dart:114–136`); no tool-string digest pin. **Proposed: nothing** (stay `open`).
- **`epic-4-retro-item-7-split-dispenser-view-arms-before-epic-5`** (yaml: `done`): landed outside this range, `95dc9dd`. Screen still split (`dispenser_screen.dart:737–763`). **Proposed: done** (already).
- **`epic-5-choir-pilot-run-and-measure`** (yaml was `open`): **no evidence found** of a measured run. Scoreboard empty. **Party 2026-09-09, Sergio: close as not-run.** **Proposed status: `done`** (user confirmed; `--set-action-status` applied).

## Action items

Proposals; not auto-applied as code. Owners named. Party 2026-09-09: items 1–3 **and** the detector notice **before Epic 6 opens** ("bugs primero").

1. **Scan landing epoch parity with genesis, and honest delivery.** Copy genesis's in-queue `_epoch` guards into `_appendScanLanded` / `_appendScanSliceFailed`; do not return `ScanConsentDelivered` if the landing `catchError` swallowed a store failure; add the genesis queued-landing-close pin on `ScanController`. *Owner: dev-loop.* Sources: F-D1, verification-gap F-R scan landing. **Before Epic 6.**
2. **Abort the in-flight wire POST when scan/genesis `close()` runs.** Thread close into `sendSlicerWire`'s `abortTrigger` (or equivalent) so abandonment cancels transport, not only the local cache. *Owner: dev-loop.* Source: F-S3. **Before Epic 6.**
3. **Backgrounding during an in-flight shoot still closes (or unlinks) the scan.** `scan_screen.dart` lifecycle currently skips close while `_shooting`. *Owner: dev-loop.* Source: F-R1. **Before Epic 6.**
4. **Face-gate demoted: courtesy, not load-bearing.** Stay face-only; no pose/object composition; no APK bloat. Update `face_gate_port.dart` and `face-gate/BAR.md` (drop “reopens before 5.5”; residual FN is not a blocking finding). *Owner: docs / next small pass.* Source: F-S1, party 2026-09-09. **Ruled. Not on the pre-Epic 6 bug train.**
5. **Functioning failures are communicated** (not anti-frustration). Detector-error-past-retry **and** missed shot (`CameraShotNone`) join the `scanOpenFailed` notice (same path as `CameraShotAccessLost`); no `permission_refused`/`face_refused`; no quiet pop. Widget pins in `scan_screen_test.dart`. Principle in `project-context.md`. *Owner: dev-loop.* Source: F-S2, party 2026-09-09. **Before Epic 6.**
6. **Reconcile AD-11 in the architecture spine** (four decided channels; camera permission is ours, capture stays plugin-served) **and delete the stale "reopens before 5.5" sentence** in `face_gate_port.dart`. *Owner: docs / next planning pass.* Sources: F-A1, F-A3.
7. **Choir pilot: not run.** Closed 2026-09-09. Do not invent credits. Epic 6's working mode is decided at its door without a scoreboard. The diet still splits that epic at the door. *Owner: sprint planning.* Source: F-P1. **Done (not-run).**

8. **Story diet is standing, not a pilot.** Re-partition **at each epic's door** under the 24 KB presentation gate (not a bulk split of later epics). Do not wait for a choir verdict to keep splitting. Already in `project-context.md`; this retro records the empirical pass. Sergio, party 2026-09-09: "la primera, cada épica al abrirla." *Owner: sprint planning.* Source: F-P3. No new sprint-status row — the policy already exists.

Deferred (ledger, not new sprint rows unless already there): F-A2 LocalSlicer `is` check; F-D2 write-path extract; F-D4 dead `writeScanCappedCopy`; F-R2/F4 androidTest; F-R4 unlink asserts; F-R5 slack widget pin; F-R6 `ResolutionPreset.max`; F6 hardening backlog; plugin camera-cache sweep; truncated-landing as seasonal dormant (related to item 1).

## Acceptance verdict

**accepted-with-open-items** (criteria: **declared**).

- All thirteen stories `done`; `pending_stories` empty — no forced rejection.
- Declared FR coverage (FR-11 Epic half, FR-13, FR-15, FR-16, FR-25, FR-31 curation half) is met on the common path: both genesis entrances, per-scan consent with equal pair, on-device face gate before any `ScanConsent` mint, sealed three payload shapes, scan cache unlink + sweep, Epic material in the one resolver, invisible buffers, cluster curation in three homes, seasonal suggestion ≤1/season.
- Binding residuals were **renegotiated in-story**, not silently dropped: 4 FN/12 face-only gate (5.1/5.5 P1), 12.5 MP ceiling (5.3), supersession conjunct (5.9).
- Open items 1–3 and 5 (functioning-error notices, including missed shot) are named races/tails — they do not void the shipped happy path; party 2026-09-09 scheduled them **before Epic 6**. Item 4 (face-gate) is **ruled**: courtesy, not load-bearing; no composition reopen. Item 7 (choir) is **closed as not-run**.
- Behavior: committed gate green (1111 tests). Runtime camera/scan/genesis **not** re-run on a device this session.
- A human decision may override this verdict.

## Open questions

- F4 androidTest half: keep the epic-4 item `open` (recommended) or split the id? (not this train)

## Assumptions

Omitted — interactive run. Epic 5 was user-confirmed. Phase 3 ran (party 2026-09-09): diet = JIT at each epic door; bugs 1–3 + functioning-error notices before Epic 6; anti-frustration ≠ hiding errors; face-gate is courtesy not load-bearing; choir closed as not-run.


