name: core
description: The pure-Dart functional core of the organizer app. No Flutter, no drift, no plugins (AD-5).
publish_to: none
environment:
  sdk: ^3.13.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
