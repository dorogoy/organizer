// The store seal's clean-shaped fixture: a persistence import in a file
// whose path IS on the allowlist when the scan runs (the fixture drives
// scanSource's allowlist parameter directly).

// ignore_for_file: unused_import
import 'package:drift/drift.dart';

final int insideMarker = 0;
