// The egress import seal's clean-shaped fixture: an HTTP import in a
// file whose path IS on the permit zone when the scan runs (the
// fixture drives scanDartSource's allowlist check by path).

// ignore_for_file: unused_import, uri_does_not_exist, depend_on_referenced_packages
import 'package:http/http.dart';

final int insideMarker = 0;
